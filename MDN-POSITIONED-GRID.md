# MDN positioned Grid diagnosis — September 11, 2026
- **Exactly one predicate match: `e74`, HTML `ul.a11y-menu`, no id; absolute block, `contentMode: blocks`. No matching fixed child or out-of-flow Grid container.**
- Parent: `e72`, HTML `body`, `contentMode: grid`, computed `position: static`; formatting ids are child 5 / parent 3.
- Physical computed insets: top `-320px`, right `2px`, bottom `auto`, left `2px`.
- Computed width `calc(100% - 2px * 2)`, height `auto`; min-width/min-height `auto`, max-width/max-height `none`.
- All four Grid placement longhands are `auto`; computed visibility is `visible`, displayed/visible flags true. These are styles, not tested used geometry.
- `retainedAsGridItem: false`; the node remains in its parent's formatting children and order-modified children. Do not turn it into an in-flow track-sizing item.
- **Smallest next production step:** add a focused regression for this absolute, non-Grid child with all-auto placement and a static Grid parent; integrate correct absolute containing-block selection and existing sizing/insets with Grid flow. The actual containing block was not measured: do not assume it is the static parent. Neither fixed-position support nor positioned Grid-container support is indicated; do not merely skip the guard.
- The previous first20 non-coordinated references match exactly: 10 HTML `li`, 8 HTML `button`, SVG `svg` at `e102`, and HTML `iframe` at `e1927`; every ref/tag/namespace mapping is retained in `METADATA.json`.
- Formatting: 1,753 boxes **not used geometry**, 54 non-coordinated deferred nodes; 10 Grid shells / 47 Grid-item flags. CSS work 2,894,159/5,000,000; property/at-rule/value/selector/media issue counts 261/14/25/29/24 remain. Removing this first barrier does not establish MDN renderability.
- New frozen build only: `native-grid-overflow-september11-round01/snapshot01`, independently verified **8,107 passed / 0 failed / 1 existing exclusion**, 131 selected / 130 strict, 1,017 source / 1,812 compiled files; no reattribution of the earlier 8,095-build click failure.
- One native fixture navigation and one formatting pass; **zero clicks, geometry calls, retries or wire requests**. All 19 original responses preserve URL/status/headers/body bytes (270,288 decoded bytes; 19 mocked transport requests, zero encoded bytes).
- Original limits/guards retained: 30+5 seconds, 6 MiB, viewport 1,280×720; child exited zero in 400 ms. Metadata accesses only id/class attributes, requested styles and native identities—not values/passwords, arbitrary attributes or storage/cookie contents.
- Cleanup complete: session/transport/document closed, zero pending loads/active transport/retained nodes, unchanged descriptors, process group gone; all source/compiled pins and 138 historical receipt entries preserved. No previous report/evidence, production source, test, manifest, TASKS or commit changed.
- Evidence: `node_modules/.cache/native-validation/native-mdn-positioned-grid-september11/METADATA.json`, `RESULT.json`, `PREFLIGHT.json`, `INTEGRITY.json`, `VERIFICATION.json`, `RECEIPTS.sha256`; source/compiled inventory hashes are pinned there.
