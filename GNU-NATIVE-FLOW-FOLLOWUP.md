# GNU native follow-up — September 11, 2026

- **Corrected preflight passes; the first actual GNU attempt stops at the unchanged8-request guard before the initial page commits.** This is a bounded live failure, not a website access denial, successful click, rendering pass or working-site certification. No website retry, fallback navigation or guard bypass occurs.
- Only the new lane corrects the path comparison: every strict root must start with literal `snapshot01/`; that exact prefix is removed before comparing with native manifest entries. All other release checks remain. Eight syntax-only checks pass before GO; the probe, transport guards, supervisor launcher, common helpers and seccomp wrapper remain byte-identical to the original GNU lane. Original `GNU-NATIVE-FLOW.md`, all24 old lane files and its lock remain unchanged; its prior failure remains a zero-website-activity preflight outcome.
- Runtime is the in-place `native-table-document-integration-september11-round04/snapshot01/dist`, release `2e88c1464b0975f1ad8a9e3e4eca122e648175b8`. Preflight verifies AUDIT, all20 gate receipt pins,24 release-owned source inputs plus the native manifest against commit, and1059 source/1900 compiled inventory entries. No current working source is imported and no runtime snapshot is copied or rebuilt.
- **Historical gate only:**10123 passes/0 failures/2 unchanged exclusions,170 selected/169 strict,574 manifest entries; original gate UTC `2026-09-11T20:53:04.875Z`–`2026-09-11T20:55:20.562Z`. No historical build, test, strict, format or audit command is rerun.
- Source inventory SHA-256 `eddbf8d3976c022f0286b48bd958dfceab99b8cb5df8810319ba9bc6f350049b`; compiled `ed56ad5206a7fa7eeac5fb8a4aa00c8f23a3e6421387507a3a50a5f3c1d467cf`; AUDIT `0917ce1c345170f014885199f3abf475dbc3dba9f04285bcf5d635c2bc6fab22`; actual audit source `f877ff7fd87a2563a0bab0fdae2ae8ddc6ba6203484ab8ebd5ba2b1c4feaa611`. Source/dist ledgers, gate inputs and harness pins match before/after.
- Exactly one GO and child: supervisor UTC `2026-09-11T21:43:50.565Z`–`2026-09-11T21:43:52.644Z`; native report UTC `2026-09-11T21:43:50.681Z`–`2026-09-11T21:43:52.634Z`. Child exits1, process group3268919 is absent, output39185 bytes, stderr0; no timeout, signal, spawn/stream error or output overflow.
- Exact first failure at `2026-09-11T21:43:52.582Z`: stage `initial-navigation:network`, `AssertionError`, `ERR_ASSERTION`, `Native request attempt cap exceeded`. The caught navigation error is separately `AgentBrowserError`, code `aborted`, message `Navigation aborted`. A further adapter entry is rejected before request-start logging/transport; its URL is not recorded at that assertion. There is no ninth wire request.
- Exactly8 recorded admitted native request attempts/8 transport requests/8 wire requests/8 HTTP200 responses/8 wire closes:1 HTML document,2 stylesheets,5 images, all from `https://www.gnu.org`. Exactly0 mocks,0 redirects,122854 encoded bytes and182240 decoded bytes. Observed request spacing is250–251ms. One navigation call,0 commits,0 native clicks; no anchor discovery or post-click formatting census is reached.
- Initial response `https://www.gnu.org/software/` is HTTP200 `text/html`,55573 decoded bytes. Read-only inspection of its unchanged captured text finds `<title>Software\n- GNU Project - Free Software Foundation</title>` and `<h2>GNU Software</h2>`; `\n` represents the actual title newline. **Native committed title/body/content classification is unavailable.** This raw capture inspection is not DOM parsing, partial rendering or a fallback page. Status/header classifiers find no access barrier or Retry-After in the eight responses; full native page-content classification never runs.
- All eight original transport-decoded bodies and hashes are retained. HTML SHA-256 `4622336763824bdd2c1cb9c43c8fb2a1ac78a6da3634b1d61cc36eb7400a9e60`; `/layout.min.css` `af5713a3ea095bac9aa3aefa0b63090c13a898a0cb6e86610658a24204be2b1b`; `/print.min.css` `bcd220f236a677578a4751a0b48819ec90ce2eb9cb833a38239415c4cb12ab42`. Five image URLs, sizes and hashes are in `OBSERVATIONS.json` and the native report. No HTML, CSS or captured response body is rewritten or selectively omitted to obtain a page.
- Bounds remain one child/nativecapacity1,8 bodyless GETs including subresources/redirects,250ms pacing,30s+5s,6MiB output/file and unchanged response/aggregate/style/DOM limits. Private empty HOME/TMP, environment allowlist, native public-address/TLS/origin guards and live seccomp remain enforced. Loader metadata requests `include`, but the adapter forces `omit`; wire guards record absent credential headers and the final cookie jar has0 accepted cookies. No scripts/SafeJS, alternate clients, providers, device/TTY, identity masking, challenge solving or access-control bypass occurs.
- Observed cleanup: document nodes0, session tabs0, pending loads0, active transport0, queue active/pending0, cleanup errors0; session/transport/queue are closed, all8 wire requests close, HOME/TMP remain empty. Immediate pendingLoads1 settles to0 after50.256873ms within the100ms observation bound. **Event/image owner sample arrays are empty because loading failed before their instrumentation returned; this is not independent proof of those owners' cleanup.** Full owner-level zero-cleanup evidence is therefore unavailable.
- **40/40 retained evidence checks pass, not the website flow.** Empty-owner-array predicates in the inherited verifier are explicitly limited as described above. Original GNU, SQLite and W3C reports and all six historical receipt ledgers verify unchanged. Pre-syntax storage has1567469568 bytes free; evidence-seal storage has1912832 allocated lane bytes and1554726912 bytes free, within12MiB lane/64MiB free gates.
- New evidence is confined to `node_modules/.cache/native-validation/native-gnu-flow-followup-september11/` and this report. Both `RECEIPTS.sha256` and `FINAL-RECEIPTS.sha256` seal named checks, original bodies, pins and report; both new run/launch locks are retained. No production/test/shared-doc/manifest edits, builds or commits occur. Parent owns the new-host inventory audit; this lane does not change host counts or assert working-site coverage.
