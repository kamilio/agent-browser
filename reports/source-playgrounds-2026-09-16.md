# Source-only React playground code — September 16, 2026

## Implementation and real recovery

Recover bounded Sandpack pre/code entries from the route-bound inert React
__NEXT_DATA__ content JSON. No script execution, dependency, rendered-code claim,
filename inference, DOM mutation or Markdown insertion. Source offsets, decoded
paths, owner association and optional source class/meta labels remain explicit.
Whole-entry metadata fits both64KiB and the existing extraction output budget.
Details: SOURCE-PLAYGROUNDS.md.

One fresh native GET returns the same React body captured before the fix. The
reader adds8 code entries across5 playgrounds, including all3 CSS occurrences
missing from the HTML, as3862 JSON metadata bytes. Existing
17,992-byte Markdown and all other extraction fields match after only opaque-ref
normalization. Source code/path/offset comparisons independently match the saved
payload. The repeated button CSS remains two distinct associated occurrences.

## Executed validation

- Core02:5,022 passed/0 failed in44 explicit native-manifest files, including106
  new collector/integration cases. Build, strict test types, format and lint pass.
- Full02:46,100 passed/0 failed in937 available committed-manifest files,
  592.542 seconds.959 entries still include22 missing committed files;
  this is not full-manifest acceptance, actual SafeJS or device validation.
-132 saved-body comparisons:127 successful pairs preserve identical Markdown,
  previous extraction fields, reader counters, DOM and classifications. Only
  React gains the optional metadata. Five pairs match the long reader's existing
  non-HTML refusal: three historical inputs and the two new Markdown pages. The
  latter independently pass native CLI/default-profile Markdown checks; their
  long-profile rejection is not successful content coverage or a new regression.
- A synthetic kernel/JS-network-denied CLI proof checks the actual feature before
  the one fresh React GET. All recorded children/groups close. No scripts, SDK,
  credentials, devices, TTY or challenge solver are used.
- Independent static review finds no blocking defect. Its two nonblocking gaps
  remain explicit: the integration abort fixture reaches outer decoding, while
  unit cases cover both boundaries; combined optional-metadata budget competition
  lacks a dedicated integration case. No broader fidelity or speed claim follows.

## Preserved failures and open goal

Core01 has5,020 passes and2 invalid-URL fixture failures. Correct required URL
arguments and lint-only regex/Infinity forms; retain the original run and type
diagnostics. The first differential stops after127 records on a guard-specific
PyTorch target-capture schema. Replay02 explicitly validates that pinned body's
base64, size and hash without changing the standard capture decoder or originals.
EXECUTION-NOTES.md retains these distinctions; none is a hidden production fix.

Preserve42 pre-existing tracked edits and697 untracked files. The new manifest
entries are admitted only for this owned change; unrelated working-only tests
are not folded in. The complete browser goal, SafeJS scheduling, rendering,
research, credential/passkey/device/TTY and website-access/CAPTCHA gates remain.
No push is claimed. Evidence: node_modules/.cache/native-validation/source-playgrounds-september16/.
