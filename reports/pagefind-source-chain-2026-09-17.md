# Git Pagefind source chain — September 17, 2026

Four fresh anonymous native GETs reach the search application script, Pagefind
module, entry JSON and English binary metadata. This advances the investigation
beyond an unseen computed import to concrete source data. **It does not establish
working Pagefind search, execute JavaScript/WebAssembly or retrieve442 pages.**

## Current-state correction and request scope

The older source-search-assets report was not the latest checkpoint: Python's
complete3,927,697-byte index, five literal queries and two linked documents were
already validated in the later source-index-search report. The Git UI script was
also already captured. A proposed duplicate Python acquisition is cancelled
before any child or GET; an initial preparation seal-schema assertion and the
superseded plan are retained. Neither Python nor the prior Git UI is refetched.

The new application URL is a literal script src in saved Git search HTML. Its
filename hash matches the newly retrieved body. The module URL is initially a
conditional derivation from the saved UI's default /pagefind/ base and explicit
pagefind.js suffix, not a literal HTML link. The separately captured application
contains a PagefindUI constructor without a bundlePath override and a separate
computed loader that conditionally yields the same module path. Source flow is
inspected, not executed; runtime mutation and uninspected branches remain unknown.

The module exposes an entry JSON filename. This acquisition omits the runtime
cache-tag/time query deliberately; it does not reproduce an executed fetch or
prove a coherent deployment snapshot. English metadata uses the exact captured
entry hash plus the observed module filename template—no hash-dependent shard
name is invented. English is selected explicitly, not through executed detection.

## Observed acquisitions

| Resource | UTC response time | Body bytes | Native outcome |
| --- | --- | ---: | --- |
| application | 2026-09-17T09:58:23.028Z | 15229 | failure / unsupported MIME |
| module | 2026-09-17T09:55:18.205Z | 45555 | failure / unsupported MIME |
| entry | 2026-09-17T10:00:38.118Z | 2187 | extracted-unverified |
| metadata | 2026-09-17T10:04:13.184Z | 4419 | failure / unsupported MIME |

Three native reader failures remain **unsupported MIME at the loader stage**.
Those complete source captures are not promoted to successful documents. The
entry JSON uses the existing root-pointer selection; its entire source text is
preserved exactly inside a Markdown code block, with extracted-unverified status.
No CAPTCHA/access barrier is reported for these four responses; this is not
evidence that crawler blocking or challenge handling is solved elsewhere.

## What the source establishes

- The application supplies subresults/images/filter/ranking options, but no
  bundlePath override for the observed constructor. Language is query-derived;
  no initialization, query trigger or callback is executed.
- Module and entry source both report version1.5.2. The entry contains34 language
  descriptors; English reports hash en_dcc1cde1fe, wasm label en and page_count442.
  The count is source metadata, not442 fetched, working or validated pages.
- The module has hash-based metadata/index/filter/fragment loaders and a WASM
  backend. No WASM file is acquired or executed, and no result fragment is fetched.
- The4419-byte captured metadata body is itself gzip data after transport
  decoding. Bounded decompression yields7901 bytes: a12-byte pagefind_dcd signature
  and7889-byte CBOR payload.
- The existing authentication-oriented CBOR whole-value codec correctly refuses
  its7609-byte input bound. That limit is unchanged. Separate prefix calls decode
  the observed six-item root array, each field within the original limits, and
  consume the entire payload. This is exploratory syntax inspection, not a
  production source parser or independently validated semantic schema.
- The decoded layout contains version1.5.2,442 hash/integer pairs,57
  string/string/hash triples, one key/hash pair, an empty array and four strings.
  Interpreting the triples as term-range/chunk records is an **inference**. One
  contains literal rebase between pushr and ref with hash en_cc40c7f; its chunk
  is not fetched and no posting, document or fragment is resolved from it.

## Validation and retained limitations

All four GETs use the same qualified release02 runtime, source-equal to committed
3ce7127:1604 source and2380 compiled pins verify. Its3804 selected native tests
and scoped quality results are prior evidence, not new runs or full-suite approval.
No production browser code, dependency or native test manifest changes here.

Each live command has a matching synthetic route/closure proof. Synthetic fixtures
are not publisher responses or search-success evidence. Existing2MB response,
256KB extraction,256MiB heap,16MiB per-file and60-second live supervision remain.
No retries, redirects, credentials, SDK, TTY, forms, subresource auto-loading,
identity changes or access-barrier bypass. Input/runtime and live/proof closure
checks pass; the four acquisitions are separately scoped, not automatic navigation.

The first entry JSON proof supervisor wrongly inherited a zero-reader expectation
from the preceding MIME-refusal probe. Its actual JSON operation succeeded; the
failed supervisor result remains, and a separate corrected proof precedes the
sole live GET. Application preparation's redacted-query assertion, the whole-value
CBOR bound and a pre-Node guard-file collision are retained. None caused a repeated
website request. Local decompression/prefix probes are guarded exploratory data
inspection, not a native unit-suite or actual script-runtime acceptance result.

## Next functionality work

Add bounded non-authentication source-data parsing and validate literal query
selection through actual postings and result fragments, retaining provenance and
original native outcomes. Do not enlarge credential-protocol limits or present a
source-only lookup as the site's stemming/ranking/UI implementation. Actual
SafeJS/modules/WASM and interactive-browser qualification remain separate work.

Broader performance, different websites, access/CAPTCHA handling, real credentials/
passkeys/devices/TTY and unfinished topic research are still open. Historical
100-entry33-useful/67-other outcomes remain unchanged. The overall goal is active.

## Evidence

The adjacent JSON records URLs, times, hashes, source derivations and probe scope.
The four acquisition lanes—git-search-init-september17, git-search-module-september17,
git-search-entry-september17 and git-search-metadata-september17—are sealed under
`node_modules/.cache/native-validation/`. Parent audit/publication evidence is in
`node_modules/.cache/native-validation/source-search-completion-september17/`.
No historical result is rewritten to imply a fresh validation.
