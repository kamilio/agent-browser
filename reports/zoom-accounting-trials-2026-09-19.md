# Zoom accounting trials: initialization remains blocked

September 19, 2026. Zoom is not joined. No meeting UI, admission, incoming audio,
recording, transcript or delivered notes are demonstrated. All new executions
below are isolated offline checks; the last live run remains the September 18
capture documented in `zoom-unicode-initialization-timeout-2026-09-18.md`.

## Qualified changes

The tracked-literal contribution reuses existing descriptor projections and
repairs runtime snapshot recognition of exactly branded tracked objects. It does
not permit untrusted proxy transport or cache mutable descendant totals. The
combined private package passes nine actual native/runtime cases, but the exact
captured vendor still times out at 120.230 seconds and 827,172 SDK steps.
See `../contributions/safejs-literal-accounting/README.md` for qualified and
unavailable tests; three CLI-related suites remain unavailable, not passing.

The next change avoids entering the retained-data visitor for undefined capture
values. A finite owned fixture reduces 1,003 visitor calls to three while keeping
data11, one provider call, all1,001 yields and iterator completion unchanged.
Seven correctness cases, 125 related tests, strict types and a fresh build pass.
The nine native/runtime cases pass again in 11.986 seconds with all resources
closed. See `../contributions/safejs-absent-captures/README.md`.

That subsequent vendor replay still times out at **120.278 seconds**, with
**825,291 SDK steps**, **800,003 peak data units** and depth18. Evidence is
`/tmp/agent-browser-absent-vendor-4mbworbo`. Parent verifies7,084 input hashes and
19 artifacts. No outer timeout/signal, network attempt or cleanup rejection
occurs; retained data, pending callbacks, tabs and requests are zero after close.
These isolated improvements do not establish an end-to-end speedup.

## Aggregate diagnostic

One separate observer-instrumented run deliberately cancels after30 seconds.
It records26,576 accounting walks and160,508,785 visitor calls, including
40,006,263 uncharged primitive returns,54,043,965 already-seen object returns,
18,119,951 owned closures and20,146,612 scope roots. Only1,762,883 visits take
the new object-literal identity path. Categories are branch counts, not mutually
exclusive timing buckets or CPU percentages.

Evidence: `/tmp/agent-browser-accounting-counter-diagnostic-a0jjjS`. Parent
verifies7,085 input hashes/modes and20 artifacts. The process exits1 for its
explicit observer-requested abort; it is neither an asset success nor a120s
timeout reproduction. Its observer overhead makes timing comparisons invalid.
All observed resources close; final retained data is zero. The captured AST
span fields are null because the observer expected the wrong span shape; they
do not identify a source location or exact suspended instruction.

## Remaining work

Repeated retained-graph measurement remains the demonstrated hotspot. Broad
descendant caching, provider deduplication and skipping scope traversal are not
justified: mutable captures, provider ordering, aliases, weak collections and
depth checks must remain correct. The ordinary-literal fast-path audit also
finds insufficient coverage and rebranding/order risks for unconditional use.

The recorded live target already is Zoom's genuine browser client; directly
choosing that route is not an untried workaround. No host setting, authentication,
passcode, waiting room or recording permission is bypassed. Further optimization
must demonstrate useful progress on the actual client before a live retry.
The native engine remains independent of Chromium/Firefox/remote browsers.

These changes are local contribution patches and private qualified SDK packages,
not publication, default runtime activation or a completed Zoom implementation.
