# Developer documentation and repository content — September 16, 2026

## Outcome

Three exact public developer pages return HTTP200 and useful source-backed content through the committed native browser. All **53 fenced code blocks** match independently decoded source PRE text. No page scripts, credentials, retries, redirects, alternate clients, fingerprint changes or challenge solving are used. These are task-relevant additions, not a new agent-visit ranking or revised historical100-page verdicts.

**MDN is not an unqualified content pass:** independent review identifies two missing experimental-method warnings and a missing compatibility fallback explanation. Native saved-source diagnosis reproduces these gaps; they remain unresolved. Python and GitHub are useful with the scope/context qualifications below.

| Tested page | Decoded body bytes | Main Markdown bytes | Exact code blocks |
| --- | ---: | ---: | ---: |
| https://docs.python.org/3/library/asyncio-task.html |177,603|65,798|34|
| https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise |201,396|40,101|18|
| https://github.com/kamilio/agent-browser |577,644|76,842|1|

Each outcome remains extracted-unverified/contentSuccess:null. Code/text fidelity does not verify the documentation's claims, compatibility tables, example correctness, current local repository state or repository build. Full source and Markdown remain in hash-pinned local captures; the companion JSON records exact URLs, source identity, review scope, counters and hashes.

## Practical improvement

For a README-only task, the existing actual CLI with **--selector article** returns32,630 bytes instead of76,842. The source has one matching article; all32,630 bytes are an exact substring of the main-content output. This saves44,212 output bytes (**57.54%**) while retaining the entire selected README. It deliberately omits the repository file listing and other main-content sections.

This is a validated explicit workflow, not a production-code or default-selection change. It does not reduce downloaded bytes or establish faster networking. The actual CLI check routes the single saved response under kernel-denied networking and makes no extra GET. See DEVELOPER-CONTENT.md for commands and the distinction between --selector and heading-only --section.

## Live and offline evidence

- Runtime commit:bbe5ac1b7c1f7fab115454ed4d278e2f4e404ac8; all1531 committed source/config/manifest inputs match the pinned clean build. Pre-existing dirty runtime is not used.
- Exactly three native anonymous HTTPS GETs, with recorded request starts at15:03:55.354Z,15:03:57.882Z and15:04:00.306Z on September16. Requests are sequential, separated by more than two seconds; no redirects/retries. Total956,643 decoded/127,787 encoded bytes.
- Three actual-CLI synthetic routed proofs precede live admission under kernel network denial. They prove the request/output boundary only, not real content.
- All three captured responses replay through the actual CLI with complete extraction, reader and classification equality. One separate saved-response CLI selection verifies README reduction.
- Independent Python standard-library HTMLParser inspection compares every PRE in the selected main scopes against every emitted fence in source order:34+18+1 exact. Only source line endings, Markdown container prefixes and a required final fence newline are normalized; no indentation/content trimming. Parser checks run under kernel network denial, not a claimed JavaScript guard or page runtime.
- Native transport, observed request/socket/TLS connections and child groups close; certificates are authorized, credential headers absent, HOME/TMP empty. Live CLI does not separately expose document-instance closure counts. Per-request timings are single observations, not a controlled speed benchmark.
- The unchanged runtime's prior2,963-case/34-file native gate and build/types/format/lint pass are pinned references, not a fresh unit run or full-manifest/SDK claim.

## MDN diagnostic, not a content failure

Four native tokenizer issues correspond to exact empty **<?>** source markers, already tolerated by the reader. Instrumented saved-response replay observes the same four marker positions in each of two source-tokenizer instances: unfiltered visibility preflight and filtered loading. Complete extraction remains identical. Removing preflight, hiding the count or treating8 instrumentation events as8 source defects would be incorrect.

Initial attempts remain in the evidence: a Python3.8 comparator API mismatch; a comparator that omitted role=main scopes; a prototype observer that could not intercept an instance arrow function; and a diagnostic assertion that expected one pass instead of two. These are corrected audit assumptions, not fixes to the browser. No additional live request is used for diagnosis.

## Independent review and remaining work

Independent review of non-code prose, selection and source qualifiers is retained in content-review.md/JSON in the evidence lane and embedded in the companion report. Python's287 prose paragraphs,41 definition terms,17 headings and250 text links survive the review's normalization; exact document version is metadata-only. GitHub's81 non-code README paragraphs and three headings survive. Inspection limits remain explicit, including sampled human review of repetitive file-table rows rather than a full rendered audit.

**Next repair: preserve important source labels.** MDN's two method-specific warnings live on empty span elements with role=img, aria-label=Experimental and a longer title warning. Native sanitizer diagnosis confirms the2 main-scope labels survive as attributes but title warnings are discarded; ordinary extraction contains neither. There are4 such labels across the whole source, not just the2 reviewed main warnings. The generic inline-node extraction path does not emit these attribute labels. This is unrelated to the four tolerated processing markers.

MDN's compatibility widget additionally carries a template/noscript explanation that JavaScript is required. The sanitizer omits it, leaving an empty compatibility heading; no compatibility matrix exists in the captured widget. Preserve a source-scoped limitation notice without executing templates, inventing support rows or relaxing hidden/access rules. These are recorded outstanding fixes, not repaired features. Useful page content does not establish script-dependent widgets, forms, API behavior, account actions or verified facts.

The overall browser objective remains active. Earlier62 broad failures in26 files,22 missing committed test files and separate SafeJS SDK, rendering, credential/password/passkey/device/TTY/access-handoff gates remain open. No production change, dependency addition or push. Preserve42 pre-existing dirty tracked files and697 untracked files.

Evidence:/home/kjopek/project/agent-browser/node_modules/.cache/native-validation/developer-content-september16. Sealed2026-09-16T15:21:10.340Z;183 files/12003015 bytes; ARTIFACTS.json SHA256 24ac42e7282da0d01d4520f7e3bfb3106d962da2230acc3d59b467546b5e0288.
