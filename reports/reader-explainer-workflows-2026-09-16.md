# Optimized-reader explainer workflows

## Live content results

The committed namespace-only parser optimization is separately exercised on
three fresh source-linked native workflows. All three retrieve substantive text;
every emitted Markdown line is reviewed. This is availability evidence, not
medical, scientific or financial fact-checking or full interactive-page coverage.

| Publisher | Exact target | HTTP | Markdown bytes | Reviewed result |
| --- | --- | --- | ---: | --- |
| Medical News Today | https://www.medicalnewstoday.com/articles/what-can-and-cant-slow-down-aging-new-study-offers-clues | 200 → 200 |11984| Article sections, attributed quotations, source links and the article's own cautions; navigation and repeated footer/disclaimers remain |
| Biology Insights | https://biologyinsights.com/beetle-sounds-how-theyre-made-and-what-they-mean/ | 200 → 200 |3955| Explainer headings and substantive paragraphs about mechanisms, purposes and examples |
| Bankrate | https://www.bankrate.com/mortgages/the-hidden-homeownership-tax/ | 200 → 200 |2447| Research summary, headline statistics, methodology description and author biographies; chart/map values and interactive lookup are not established by this output |

Sources are the three corresponding host roots. All targets are literal links
from prior complete native homepage captures, then independently proved through
native selection/click before live admission. No alternate target or direct-target
fallback substitutes for the intended link. The Medical News Today and Biology
Insights roots were previously navigation-only; successful articles do not
rewrite those root verdicts or the original100-entry corpus totals.

Live starts are September16,2026 at11:28:52.854879,11:28:55.318335 and
11:28:57.944513 UTC. Supervised jobs take2.46,2.62 and2.53 seconds, respectively,
including network, two-second origin pacing and supervision. These observations
do not extend the earlier controlled local parser-speedup claim to networking.

## Bounded capture and replay

This batch invokes the maintained programmatic compact exact-target CLI runner
through an instrumentation wrapper. It still uses the existing native Node
transport; the wrapper does not substitute another client or alter request policy.
It records complete decoded bodies through the existing long-v1 capture helper
and retains only the existing five-header allowlist. No Set-Cookie or supplied
credential header is recorded. Anonymous bodies stay in local diagnostic artifacts,
not published report text; this is not a claim that every embedded application
string has been audited for sensitivity.

Exactly **six live GETs**, six complete captures totaling3121776 decoded bytes,
zero redirects and zero retries. Each source and destination body hash agrees
with the CLI report. All observed requests/sockets, documents and child groups
close normally; HOME/TMP stay empty. No credentials, SDK, page scripts, alternate
browser/client, identity change, account/form/cart action or challenge solving
is used. No Bankrate ZIP lookup, findings download or linked medical study is
requested.

Before live access, three saved-source/synthetic-target proofs exercise native
clicks and the capture wrapper under kernel-denied network. A synthetic Set-Cookie
sentinel is absent from retained capture headers. Afterward, all three freshly
captured source/target pairs are replayed through native clicks, again with
kernel-denied network. They exactly reproduce live Markdown and complete
normalized extraction metadata, including selection discovery counts.

These two offline phases make twelve mocked GETs and zero new website requests.
Resource safety and replay equality do not establish factual correctness or
complete rendered content. Native outcomes remain extracted-unverified,
contentSuccess null and partial reader provenance.

## Bankrate chart gap

A separate bounded static audit inspects the saved source, not a running page.
The state map has a client-rendering bailout placeholder; the risk chart has an
empty chart container. Two Flight component records reference OverpaymentMap
and RiskChart with empty props objects. Main-agent strict JSON parsing independently
confirms both empty objects and the module references without evaluating script.
Five referenced external JavaScript chunks remain uninspected. Therefore the
missing data might be constants in those chunks, later requests or computation;
its location and availability are not established here.

There are no lexical data-json attribute candidates in the decoded source. The
existing source-table collector's labeled-items schema does not apply. The audit
identifies only four partial numerical prose observations, not a chart dataset:
two income-related statements are in reader output, and two loan-type statements
are in the outside-main download section. Different measures and repeated
serialized copies must not be merged into invented chart rows.

The download section presents a report-request form with four required fields,
including work email, and a disabled submit button in the captured markup. No
direct report link is established. The ZIP lookup is also not exercised. This
is not evidence of a paywall, successful form delivery or an authorized workaround.
No explicit restriction on the displayed chart itself is identified in the
targeted markup, but external implementation and server behavior remain unknown.

The useful next diagnostic is bounded inspection of the actual advertised chart
implementation under a separate read-only scope, not a generic extractor over
empty props. A future collector needs an actual inert dataset fixture, supported
schema, bounded fields/rows and provenance before any content claim. This audit
does not prove data absence under every encoding or close the dynamic-page gate.

## Runtime and scope

All1519 committed source/script/config inputs at
`b68d802fd8d8d8fd5622306d0f4b3f4fe5bae828` match the pinned clean optimized build.
No production patch, new build or fresh unit-test run is needed for this batch.
The prior1236pass/0 selected25-file native gate and passing quality checks remain
separate from these new live/replay results; the full941-file manifest, actual
SDK, scripted websites, rendering and credential/device gates remain open.

Command limits remain4MB decoded per response,8MB aggregate, two GETs,256KB
extraction and512KB JSONL. The supervisor keeps a60-second deadline,256MiB
old-space heap limit and16MiB per-file bound. No limits or policies are relaxed.

Individual content reviews, invocation/safety records and complete body captures
are retained under
`node_modules/.cache/native-validation/reader-explainer-workflows-september16/`.
The adjacent JSON pins exact evidence. Historical results remain unchanged;
the full browser goal stays active, and no push occurs.
