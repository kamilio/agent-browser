# Core48 Zoom entry request timeout

September18,2026. The first live load after the eight-case legacy Event SDK
acceptance does not reach page scripts. Its initial HTTPS GET hits the unchanged
15-second native request timeout; total live execution is15.390 seconds.
There is no HTTP response, redirect, HTML, committed document or live SDK realm.
The script17 repair therefore cannot be assessed by this attempt.

The preceding zero-network rehearsal passes in1.267 seconds with full closure.
The live request closes1/1; queues/exchanges, cookies/storage and retained work
reach zero. Process/group are absent and HOME/TMP empty. The supervisor does not
time out or send signals. No timeout increase, added origin exclusion, source/CSP
change, credentials, clicks, media or alternate browser is used.

Parent verifies9,857 input hashes and41 artifact hashes/sizes/modes. Evidence:
`/tmp/agent-browser-legacy-event-live-september18-dkl8kU/HANDOFF.md`, with
`RESULT.json`, `CLOSURE.json`, `SCRIPT-ORDER.json` and `ARTIFACT-PINS.json` there.
Parent verification is retained in
`node_modules/.cache/native-validation/zoom-runtime-september18/DKL8KU-PARENT-VERIFIED.json`.

One explicitly disclosed, separately bounded retry is authorized after a delay,
in a fresh evidence directory with unchanged source/runtime/guards/limits. Its
outcome is separate from this preserved failure. This attempt is not a native
compatibility regression, an Event acceptance pass, or a joined Zoom meeting.

The separately authorized retry later receives the document and runs16 scripts;
script17 fails at a Unicode regexp compilation quota. That new result is recorded
in `reports/zoom-unicode-regex-budget-2026-09-18.md`, not substituted for this
response-less timeout.
