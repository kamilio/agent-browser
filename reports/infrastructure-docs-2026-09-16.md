# Infrastructure documentation — September 16, 2026

## Actual browsing

Four anonymous public GETs use the committed native runtime 59e92c0d412e280193206a76e7c2cff76451a168.
NGINX proxy-module and Docker multistage pages return readable source documentation.
The PyTorch stable CUDA path returns HTTP200 but only a redirect stub, not CUDA
content. No scripts execute. A visible Continue anchor, agreeing with source
canonical/meta hints, identifies its versioned target. The maintained native
research-link CLI loads the captured stub without another GET, selects the exact
anchor and dispatches mousedown, mouseup and click. Its one real target GET yields
95,972 Markdown bytes. Version2.14 is the captured destination, not a latest-release
claim. This is ordinary public-link navigation, not a Cloudflare bypass.

## Content and scope

Two offline native CLI selections reuse captured bodies without network access.
NGINX #content reduces106,693 to105,638 Markdown bytes; Docker article reduces
83,068 to8,486 bytes. Both are exact untruncated substrings of their full output.
This is explicit agent-selected scope, not a new automatic-focus heuristic or
a general performance result; surrounding context can be excluded by selection.

An independent static HTMLParser audit compares all109 NGINX,9 Docker and58
PyTorch source pre blocks with native Markdown fences, including occurrence counts.
All176 match interior text and indentation under stated CR/boundary-newline and
Markdown-prefix normalization. This does not validate rendered visibility, all prose
or qualifications, execute examples, or establish their correctness. The original
Python3.8 removesuffix failure is retained; the corrected audit uses endswith/slice.

## Regression validation

Three new tests cover an explicit redirect-stub anchor, absent-anchor refusal,
and conflict between the chosen anchor and script/meta/canonical hints. They use
native click behavior rather than automatic script/meta navigation. All238 tests
in two explicit native-manifest files pass, with build/types/format/lint passing.
All2,320 production compiled artifacts match the previously tested live runtime.
This is not another full native run. Its prior baseline remains45,991/0 in935
available files;22 committed-manifest files remain missing.

## Preserved failures and provenance limitation

The first follow launch failed before creating a child or issuing a GET because
the reused supervisor's hostname reservation collided with the stable-page visit.
The separate already-scoped versioned target receives its own exact-URL budget;
the original reservation and failed launch remain untouched.

The successful follow02 receipt incorrectly hashes FOLLOW-ADMISSION.json rather
than its intended FOLLOW02-ADMISSION.json. Supplemental comparison verifies that
the actual invocation pins match the intended plan and preceding offline proof,
and both plans name the same scope/target. This does not erase the historical
linkage defect. Original receipts are not rewritten and no GET is repeated for
bookkeeping. See FOLLOW-PROVENANCE-AUDIT.md/JSON in the evidence directory.

## Remaining work

The historical100-page results are unchanged. Dynamic-site/SafeJS contract,
rendering, credential/passkey/device/TTY, research and access/challenge gates remain
open; HTTP200 and nonempty extraction are not general website acceptance.
42 pre-existing tracked edits and697 untracked files remain outside this change.
No push or completion of the browser goal is claimed. Detailed inputs, captures,
timestamps, hashes, failures and scope are in the adjacent JSON and
node_modules/.cache/native-validation/infrastructure-docs-september16/.
