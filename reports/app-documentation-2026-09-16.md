# Application documentation source workflows — September 16, 2026

## Five real native requests

React Learn, Next.js installation and Transformers installation each receive one
anonymous GET on the committed source reader. All return useful source content,
not demonstrated interactive applications. React produces17,992 Markdown bytes;
Transformers9,902. Next.js answers the reader's existing content negotiation with
text/markdown:14,159 body bytes remain exactly preserved inside a literal fence.
Source versions/dates are captured labels, not verified latest-release claims.

The native saved-response API identifies the installation page's AI coding agents
link at source line390,column227. One separately scoped native GET retrieves that
public guide as15,001 Markdown body bytes, again exactly preserved. This is
source-link discovery followed by navigation, not a DOM click or page execution.
A1,132-byte module-alias source section is recovered as4,774 JSONL bytes with
exact original lines328–369 and no new GET. Source prompts are inert data.

Independent offline comparison finds all24 React and12 Transformers HTML code
blocks preserved under explicit boundary-newline normalization. The checked prose
survives. Menus, signup text, duplicate source image descriptions, toolbar text
and unlabelled fences remain; no rendered visibility or example execution is
verified. Source classes can be misleading, so they are not treated as guaranteed
language labels. React's generic sp-javascript class also occurs on a CSS example.

## Found and fixed: supporting React playground source

The React inert payload contains three supporting CSS occurrences absent from its
HTML code blocks. The new bounded source-only collector recovers these alongside
their five associated JavaScript blocks. A fifth, post-change native GET at
2026-09-16T20:11:33.079Z returns the identical265,160-byte React body as the
2026-09-16T19:49:49.780Z source. All prior extraction fields remain identical
after opaque-reference normalization; only sourcePlaygrounds is added. All eight
entries match independently decoded source. This is not a hydrated playground.

See SOURCE-PLAYGROUNDS.md and reports/source-playgrounds-2026-09-16.md for code,
limits, native validation, replay coverage and remaining acceptance gaps.

## Evidence limits

The first offline Markdown workflow used the wrong admission-profile identifier
and failed before link discovery. Corrected default-profile execution passes;
both attempts remain. No source capture is rewritten and no GET repeats for that
harness correction. All live children/transports close with no credentials,
redirects, retries, source-code execution, alternate browser or challenge bypass.
Historical100-page verdicts and unfinished research/runtime/access gates remain.
Evidence: node_modules/.cache/native-validation/app-docs-source-september16/ and
node_modules/.cache/native-validation/source-playgrounds-september16/.
