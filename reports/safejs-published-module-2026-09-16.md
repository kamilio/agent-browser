# SafeJS 0.1.640: verified acquisition and module integration review

## Result

The native browser observes `@poe-platform/safe-js@0.1.640` under the registry's
latest tag at **2026-09-16T23:44:29.022Z**. The complete3188-byte metadata body is
captured while the CLI selects the exact `/version` JSON value. This is a dated
observation, not a promise that a moving tag remains unchanged.

One further native GET retrieves the exact published tarball. SHA512 integrity
and the published SHA1 match. Passive bounded extraction produces380 files and
43,728,393 bytes in a new private temporary directory, outside browser/project
dependency resolution. Every extracted file is inventoried and rehashed.

**No SDK code is run or activated.** No project dependency is changed; required
dependency closure is not staged. This refresh supplies verified package bytes
and newly available public API evidence, not dynamic-website acceptance.

## Native acquisition

| Request | HTTP | Decoded bytes | Encoded bytes |
| --- | ---: | ---: | ---: |
| Exact registry latest metadata | 200 | 3,188 | 1,424 |
| Published safe-js-0.1.640.tgz | 200 | 8,666,098 | 8,666,098 |

Both use the protected native runtime matching browser commit`c919925` and its
recorded2581-case focused/quality gates. No native suite is rerun for this source
refresh. Each real request has one authorized TLS connection and matching request/
socket/process closure. Requests have no supplied cookies/credentials, redirects,
retries, page scripts, other browser or challenge solver. Server-returned cookie
values stay in private acquisition evidence and are not republished here.

Exact-command in-memory controls run first under kernel/JavaScript network denial.
The first archive launcher stops before child creation because a reused supervisor
requires `sourceUrl`; its receipt records no spawned process or HTTP request.
The corrected launcher retains that failure, passes a new offline control and
makes the first actual archive request. It does not retry a prior network attempt.

The archive's transport limit is16MiB; passive extraction rejects escaping paths,
links, special files, duplicate entries, oversized members and expansion beyond
64MiB. No lifecycle hooks, builds or imports occur. Integrity verifies bytes
against metadata; registry signatures/provenance attestations are not verified.

- Metadata SHA256: `3d548fc40e4d063f79c4924940afd34254e6ae5733b2619ec0b0c823ae55bbe1`
- Archive SHA256: `f464a1db14b8b08deeb558c152e93846e89e1c1664eda8237b380966278d0eeb`

## New API evidence

The extracted public declarations contain `parseSourceModule`, `SourceResolver`,
`SourceModule`, realm `sourceResolver`, and evaluation `sourceType: "module"`.
The resolver returns authorized source identities/text; it is not a network loader.

Three source-map payloads—`realm.ts`, `modules/source-graph.ts`, `extensions.ts`—
match neighboring commit`5da5de84dfbb25cd6410bec999a2d41e8146175f` byte-for-byte.
Other package sources are not declared equivalent. The neighboring private
workspace's0.0.1 version is not treated as its published package version.

A parallel static review inspects those public contracts and committed tests for
resolver ownership, denial/identity caching, source budgets, cancellation, ordered
evaluation and result behavior. Tests are read, not run. Browser comparison uses
the current local adapter files; its pre-existing page-script edits are explicitly
identified in the audit rather than qualified as committed/SDK-tested changes.

The bounded path forward is explicit in-memory programmatic module admission,
followed by separately qualified native-network resolution and HTML-module loading.
It needs source-kind/identity forwarding, dependency budgets, namespace handling,
proper cancellation and explicit legacy rejection. Current browser HTML loading
still skips modules/import maps and uses classic lifecycle semantics. Details:
`SAFEJS-SOURCE-MODULES.md` and private `MODULE-REVIEW.md`.

## Unchanged blockers

The mapped public realm retains external active-operation reentry rejection.
New module APIs do not establish the required callback-prefix/later-source progress.
The0.1.599 eleven-pass interrupted gate remains historical evidence; **there is no
actual0.1.640 SDK gate here**, successful or failed. Do not weaken its remaining
core/page-extension checks or infer that React, feeds or challenges now work.

Next work is bounded module adapter implementation and separately scoped runtime
qualification after closure staging/launcher hardening, not blind dependency
activation or another identical reentry retry. SafeJS, live scripted sites,
performance, crawler/CAPTCHA handling, authentication/devices, missing native files
and original research topics remain open. No historical website verdict changes.

Evidence: `node_modules/.cache/native-validation/safejs-published-module-september16/`.
Package bytes remain in a private temporary store, not a portable archive committed
to this repository. Preserve prior42 tracked edits and697 untracked files. The
overall browser goal remains active; no push occurs.
