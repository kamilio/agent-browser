# Unicode profile reaches a script initialization timeout

September18,2026. One fresh native Zoom load explicitly selects core50 and
`application-unicode-v1`, retaining the unchanged experimental K9Y732 SDK.
Only this new opt-in profile requests65,536 regex compilation allocation units;
source8,192 and all other interpreter, matcher, network and process caps remain
unchanged. Existing profiles are not modified.

The zero-network rehearsal passes in1.217 seconds. Live exits1 in145.164 seconds:
scripts1–16 pass; the same417,914-byte script17 reaches the120-second evaluation
deadline, observed at120,329ms including interruption/cleanup. No supervisor
timeout or signal occurs. Source SHA256:
`a67394b5849e496a457bc375c14f7441043cee097ae620482f404f9de6116828`.

The prior immediate32k regexp quota error does not recur. This timeout does not
establish an internal source position, successful completion of the bundle, or
a CPU-versus-pending-work cause. The original SDK rejection fields are accessors;
the observer correctly leaves them uninvoked rather than exposing raw values.
Page owner steps rise from243,583 before script17 to1,019,508 after interruption,
with peak logical data1,021,331 units. Those two samples are not a CPU profile.

All17 wire exchanges close; queues, exchanges, callbacks and SDK retained data
reach zero. Session/transport/owners close with no cleanup errors. Processes are
absent, HOME/TMP empty and forbidden attempts zero. Parent independently verifies
7,223 input hashes and73 artifact hashes/sizes/modes;2,863 SDK package files and
modes are included in the recorded selection. Private outputs remain0600.

Evidence: `/tmp/agent-browser-unicode-profile-live-september18-3GPljl/HANDOFF.md`,
with `live/REPORT.json`, `live/EXECUTION.json`, `FINAL-VERIFICATION.json`,
`NEXT-SOURCE.json`, `PINS.json` and `ARTIFACT-PINS.json` there. Parent receipt:
`node_modules/.cache/native-validation/zoom-runtime-september18/3GPLJL-PARENT-VERIFIED.json`.

No retry, timeout increase, source/CSP rewrite, extra filter, credential access,
click, media or alternate browser is used. The sole explicit file-paa.zoom.us
exclusion remains; unfiltered compatibility is not established. One bounded
offline isolation of the exact vendor asset, with private CPU/public-budget
observations, is the next diagnostic—not a full Zoom-context replay. Usable UI,
legitimate admission, native audio, permitted recording and notetaking remain open.
