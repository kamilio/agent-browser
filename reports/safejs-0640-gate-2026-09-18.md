# SafeJS0.1.640 actual isolated gate — September 18, 2026

## Result

The user-approved, offline attempt **stopped at public-core check12**.
The synthetic kernel/preload control passed. The real SDK completed11 checks,
then rejected a new source evaluation while a previous callback's asynchronous
tail was still pending:

> Sandbox object is already running.

The failed expectation is: “A new source evaluation progresses after a callback
prefix while its async tail remains pending.” Seven later core checks were not
reached. **The10 page checks and7 HTML-module checks did not run.** There was no
SDK retry, internal SDK patch, relaxed expectation or default-runtime switch.

This is a concrete compatibility blocker, not a pending authorization barrier.
The release is the explicitly pinned0.1.640 candidate, not a claim about the
current latest release. Native Zoom joining, media capture and notetaking are
still incomplete, independently of this callback-scheduling issue.

## Approval and dependencies

Approval task `remote-1789694091585000916-1793634` returned
`Approve scoped check`. Its original scope and receipt remain at
`safejs-guarded-adapter-september18/SCOPE.md` and
`safejs-runtime-0640-september18/APPROVAL.json` under native-validation.

The first dependency acquisition was aborted by our diagnostic observer: it
compared the HTTP client's pinned numeric address with the registry hostname.
The browser intentionally separates its vetted destination IP from the logical
HTTP Host and TLS identity. No response/artifact bytes were received. This was
not a registry denial or evidence that the browser needed weaker TLS/DNS policy.
The failed attempt and its14-package partial tree remain unchanged.

The corrected observer passed20 offline checks, then a deliberately bounded
second native acquisition retrieved the157,497-byte exact safe-fs0.1.640 archive.
Aggregate native GET attempts were **2 of16**; the missing-package scope remained
one. No metadata GET or installer/lifecycle script ran. The successful transport,
socket and child/group closed. The corrected observer retains exact Host/path,
HTTPS/public-address, credential-header and single-request checks; native origin,
address pinning, TLS/certificate and redirect policy were not weakened.

Independent and parent audits verified the completed **15-package,2,710-file,
19-link** closure,138,666,614 unpacked bytes. Archive integrity, actual manifests,
selected dependency constraints and contained symlink targets match. This proves
local integrity/consistency, not publisher signatures or registry attestations.

## Actual execution and cleanup

Execution phase:
`node_modules/.cache/native-validation/safejs-execution-0640-september18/`.
Original output root:
`/tmp/agent-browser-safejs-execution-lnwKWj/outputs/safejs-gate-LLYQj3`.

- Source/build baseline: `b2f9f9d65c7bc113c35214484d6133c61d2c77bc`;
  1,665 source and2,464 compiled pins. The SDK runner/fixtures are unchanged
  from their earlier442-case native qualification; current CLI115-case tests
  and quality checks pass. No new full-native-suite green result is asserted.
- Actual guard control exits zero; actual core fixture exits one with the
  expected package/version selected and the primary failure preserved.
- Both gate processes are reaped, both groups are absent, all cleanup and
  evidence-failure lists are empty, and terminal records agree with final reports.
- Core fixture cleanup reports no failures. Parent and both stage HOME/TMP
  remain empty; stderr is empty. Core preload reports no forbidden attempts.
- Post-run verification checks **6,855 file pins and19 link pins**. Runtime,
  source, dependency and control inputs remain unchanged.23 output files are
  archived with original locations and hashes; originals remain in place.
- No page scripts from websites, meeting admission, audio, credentials,
  passkey devices or other device/TTY workflows were exercised.

See `RESULT.json`, `PLAN.json`, `PREREQUISITES.json` and `execution/` in the
execution phase; acquisition/staging evidence remains in
`safejs-runtime-0640-september18/` and
`safejs-runtime-0640-recovery-september18/`.

## Interpretation and next work

This reproduces the same missing external-evaluation overlap behavior observed
in the historical0.1.599 attempt, now with a verified new dependency closure and
exception-safe cleanup that preserves the actual failure. Historical results
are not rewritten. Resolving it requires a supported SDK capability or a reviewed
browser scheduling integration; the remaining checks cannot be silently skipped.

The finite guard control is not a general hostile-code containment certification.
OS library paths and operator-approved inputs remain trusted; child-written
reports are not tamper-proof; the historical guard does not filter exec/clone/fork
or ioctl, and host deadlines remain cooperative. No new live-site, WebSocket,
WebRTC, general module/timing or full-notetaker acceptance is claimed. No push.
