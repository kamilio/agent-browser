# Native public deep-page workflows — September 17, 2026

Three literal article links selected from saved successful native source pages
received one fresh anonymous native GET each. All returned HTTP 200 and useful
source prose; this is not general rendered-site or interaction acceptance.

| Publisher | Request start UTC | HTML bytes | Markdown bytes | Eligible paragraphs matched |
| --- | --- | ---: | ---: | ---: |
| KBB | 04:17:36.334 | 278,145 | 13,820 | 43/43 |
| Cleveland Clinic | 04:17:39.656 | 379,050 | 25,002 | 20/20 |
| Cosmopolitan | 04:17:42.292 | 610,371 | 11,882 | 6/6 |

All times are September 17, 2026. Exact targets:

```text
https://www.kbb.com/car-advice/toyota-offers-electrification-101-to-employees-i-got-a-rare-look-inside/
https://health.clevelandclinic.org/urgent-care-vs-emergency-room
https://www.cosmopolitan.com/lifestyle/a73274505/september-monthly-horoscope-2026/
```

## What this verifies

All 69 selected article paragraphs longer than 80 normalized characters match
native Markdown, including 66 beyond the first selected paragraph. Checks use
article-specific source containers, entity decoding, Markdown link-label and
punctuation normalization, and whitespace collapse. They do not establish exact
rendered visibility, short paragraphs, list/image completeness or factual truth.
Medical, commercial and astrology claims are not independently verified.

KBB reports four tokenizer issues without observed loss in the checked paragraphs.
Cleveland Clinic lacks a main/article landmark in this source and retains ads,
related cards and other non-body text. Cosmopolitan also retains navigation and
advertisement labels. No concrete content-loss regression was established.

## Runtime and boundaries

Live requests use the sealed native runtime corresponding to commit `098f0cf`,
not the subsequent research service-backoff patch. Three exact compiled-command
offline proofs precede the three live requests. There are no retries, redirects,
replacement requests, credentials, cookie/auth headers, page scripts, alternate
browsers, identity changes or CAPTCHA solving. Request guards set redirects to
error for these tests; this does not change production redirect behavior.

All six proof/live child groups close. Live request/TLS/socket closure is recorded,
with private empty HOME/TMP and sequential starts more than two seconds after the
preceding process exited. Parent verification checks all 87 sealed worker artifacts,
source/compiled runtime manifests, admission/input pins and capture/content hashes.

An initial post-live Python review failed to parse a Z-suffixed timestamp. Its
script is preserved; normalizing the suffix fixed the offline review without
repeating a website request. All three pre-live command proofs passed first time.

Native outcomes remain `extracted-unverified`, with `contentSuccess: null`.
These checks do not establish fewer crawler blocks, actual-server 503 backoff,
CAPTCHA access or a new success rate for the historical 100-page corpus.

Machine-readable summary: `reports/public-deep-workflows-2026-09-17.json`.
Private original evidence: `node_modules/.cache/native-validation/public-deep-workflows-september17/`.
