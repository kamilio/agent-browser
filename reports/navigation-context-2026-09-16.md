# Preserve outside-article disclosures and credits

## Fixed failure

The preceding 135-response comparison and three live checks found a real content
regression despite successful extraction operations. V3 selected GearLab's article
while dropping its funding/affiliate disclosure from the surrounding main header;
React lost source credits from its main footer. The original evidence remains in
`reports/focus-corpus-2026-09-16.md` and its sealed cache lane.

Correct v3's generic local admission instead of recognizing a site or disclosure
phrase. Header/footer/aside and banner/contentinfo/complementary descendants still
cannot nominate ancillary article cards, but their admitted text and image
alternatives now prevent narrowing main. Only actual HTML nav/native navigation
role ancestry excludes outside context from this local check. Ineligible article
cards cannot conceal their own text or image alternatives. Main boundaries reset
all local ancestry; sibling state does not leak.

The single bounded traversal, visibility/namespace/skip rules, frozen metadata,
output budgets and v1/v2/global behavior remain unchanged. This deliberately
returns a wider main when it contains context, rather than adding unbounded
metadata or silently omitting it to fit an output limit. Content placed inside
navigation can still be excluded; this is not universal semantic readability.

## Native and review evidence

- Final release01: **2,581 passed / 0 failed in 38 explicitly selected files**,
  including all **109 new regression cases**. Build, strict selected-test types,
  formatting and lint pass. All processes/groups close with empty HOME/TMP.
- The same 38-file selection on old production gives **2,505 passed / 76 failed**.
  The new file contributes 69 failures/40 passes; seven existing nested-focus
  expectations intentionally change. Do not describe all 109 new cases as red.
- Earlier core01: 467/0 in five files, with passing quality checks. Core01 and
  final release01 compiled inventories are byte-identical, so its saved-response
  evidence applies to the same production runtime.
- Independent static review finds no actionable blocker in traversal, ancestry,
  eligibility, source visibility or legacy isolation. Initial review plus a final
  test-file follow-up pin the final candidate; the first publication check caught
  the worker's later test edit before any staging. Tests check both output formats, context wrappers,
  navigation-only negatives, ancestor resets, image alternatives, ineligible
  cards, source mutation/closure and exact scan bounds.

This is not a new full-native run. The canonical manifest has 968 entries and
still includes 22 missing committed files; only the 38 selected files run here.
The earlier feature's 46,488 passes and separate wrapper/count-audit caveat are
historical evidence for that earlier build, not a full pass for this correction.

## Saved website regression

Two kernel/JavaScript-network-denied checks use 137 pinned responses: the earlier
135 plus the recent GearLab capture and vLLM guide. Each uses explicit long-reader,
separate raw accounting and source-hidden-inline interpretation, not the original
policy of every receipt. Seven non-HTML inputs remain refused by this HTML-only
diagnostic path; separate Markdown/JSON retrieval is not invalidated.

- Legacy v2 before/after comparison: 130 successful pairs and seven matching
  failures. Complete extraction objects, structured results, source trees,
  reader metadata and classifications match exactly. Zero new requests.
- Corrected v3 versus v2, complete Markdown: 130 HTML pairs; v2 succeeds on 129,
  v3 on 130. The known vLLM guide is the one recovery. No new extraction failures.
- GearLab's two captured variants retain main and exactly match v2 Markdown:
  21,610 and 21,683 bytes. React retains the complete 17,992-byte main, including
  footer credits. PyPI still refines to its 20,682-byte article, and vLLM still
  yields its audited 17,451-byte article. CNET/Go article scopes remain available.
- The separately labeled prefix profile has 130 successful pairs in both
  policies. VLLM v2 uses its recorded plain-text fallback; v3 needs no fallback.
  All documents remain unmodified and close. No general speedup is claimed.

These operational comparisons are not full-corpus semantic success counts.
The original 100-entry 33-useful/67-other verdicts remain unchanged.

## Corrected-runtime CLI and fresh source acceptance

Two actual CLI controls route earlier GearLab/React captures under network denial.
They retain main and match the complete v2 Markdown byte-for-byte. Then two
sequential anonymous native GETs use the corrected protected runtime, default
document limits, v3, source-hidden-inline, separate raw accounting, UTF-8 fallback,
compact tables and table rows. No redirects, retries, cookies/credentials, scripts,
other browser, proxy rotation or solver is used. Each has one authorized TLS
connection, matching request/socket closure and closed transport/process/group.

| Page | Received September 16, 2026 UTC | HTTP | Decoded bytes | Markdown bytes | Selection |
| --- | --- | ---: | ---: | ---: | --- |
| OutdoorGearLab homepage | 23:30:49.975 | 200 | 203,576 | 21,612 | main; outside context present |
| React Learn | 23:30:52.379 | 200 | 265,160 | 17,992 | main; outside context present |

The fresh GearLab body changes its hero from an electric-bike to a hiking
promotion. Preserve that source drift. A separate standard-library HTML parse
extracts the complete 184-byte normalized funding statement from its main header
and verifies all of it in the new Markdown; the older v3 extraction omits it.
This is source-text preservation, not endorsement of the site's claims.

React's source body remains identical to the earlier capture. Its new complete
Markdown matches v2 exactly and includes both copyright and artist attribution;
the earlier v3 output omits those credits. The corrected runtime is uncommitted
at request time and is tied to the later commit through exact source/build pins.
Native outcomes remain `extracted-unverified`, with `contentSuccess: null`.
Whole-body capture is audit overhead, not a claim of transfer savings.

## Remaining work

V3 remains opt-in. Navigation contents and context outside the selected main are
not newly guaranteed; no-keyword structural selection is not full readability or
rendered visibility. Wider output may legitimately hit an existing limit rather
than discard qualifications. Dynamic/SafeJS scheduling, access/CAPTCHA handling,
full rendering, real credentials/passkeys/devices, missing test files and original
research topics remain open. Keep testing actual content, not just nonempty text.

Evidence: `node_modules/.cache/native-validation/navigation-context-september16/`.
Preserve the 42 pre-existing tracked edits and 697 untracked files separately.
This report does not embed the complete private cache captures or replace older
measurements. The overall browser goal remains active.
