# Three programming-reference workflows — September 16, 2026

## Fresh requests and useful content

Three manually selected task pages, not a measured agent-popularity ranking:
- Rust: https://doc.rust-lang.org/book/ch17-01-futures-and-syntax.html
- SQLite: https://www.sqlite.org/lang_select.html
- PostgreSQL: https://www.postgresql.org/docs/current/queries-with.html

All live jobs use committed 92ca98041868806c65f74ae7237dcdb91429c800 production bytes, verified against1538
source/config/test inputs and2316 compiled artifacts. Their prior selected native
gate is3615/0 across42 files, not a new full-suite run attributed to these visits.
There are four actual GETs: three initial requests and one explicit SQLite
large-document follow-up. No redirects, credentials, page scripts, SafeJS,
alternative browsers, identity changes or challenge solvers are used.

| Attempt | HTTP evidence | Complete decoded bytes | Markdown bytes / result |
| --- | --- | ---: | ---: |
| rust-futures | 200 | 50520 | 22697 |
| sqlite-select | 200 observed; no admitted body | incomplete | failure |
| postgresql-with | 200 | 43738 | 25873 |
| sqlite-select (long-v1) | 200 | 2033807 | 19 headings only |

Rust is received17:02:13.889 UTC and PostgreSQL17:02:19.214 UTC. Independent saved
source review checks all11 Rust and23 PostgreSQL code/console/diagnostic blocks.
No substantive code substitution, omission or duplication is found. Rust adds a
terminal LF to eight blocks; PostgreSQL's source-pre initial LF is omitted. Exact
internal text, indentation and blank lines match after those boundary differences.
Rust retains five listing captions, five filename labels, explicit non-compiling
example warnings, compiler diagnostics and TODOs. Class-marked scaffolding,
language/edition/test metadata remain unrepresented; PostgreSQL loses source
emphasis within code and retains extra navigation chrome. Useful source reading
does not establish executed code, rendered visibility or factual correctness.

## SQLite: preserve failure, recover selected content

The default attempt fails network.response-encoded at2015232 bytes against2000000;
it has no admitted body or primaryResponse. The observer records HTTP200 headers,
not a challenge. That failure is preserved and is not a default-profile success.

A separately recorded, paced follow-up explicitly chooses existing long-v1.
It receives2033807 complete bytes at17:04:35.168 UTC and yields19 native headings.
No global limit changes and no automatic retry occur. Two actual native replay
CLI calls then read the pinned receipt with zero network requests:

| Selected section | Markdown bytes | Source review |
| --- | ---: | --- |
| 2.5. Bare columns in an aggregate query | 4396 | 13/13 substantive units,2 SQL examples,17 link pairs |
| 9.3. Precedence of comma-joins and CROSS JOIN | 1654 | 11/11 substantive units,1 SQL example |

Both complete selected unit sequences match after documented Markdown whitespace
normalization. Qualifications, limitations and the nested Notes section survive;
the last section includes a page-update footer. SQL payloads omit one initial
source LF each. This is not whole-reference or SVG syntax-diagram recovery.

## Evidence and remaining limits

Initial reference lane: /home/kjopek/project/agent-browser/node_modules/.cache/native-validation/agent-reference-pages-september16; 66 sealed files,
ARTIFACTS SHA256 7eea60b7d7d4bce4f719e07112dee0b7807df01c18f0d2d8495ffded795b8a82.
SQLite follow-up lane: /home/kjopek/project/agent-browser/node_modules/.cache/native-validation/sqlite-long-reference-september16; 47 sealed files,
ARTIFACTS SHA256 b2396375817068f3f81d5dde16ef69a97f01afffc2cb872216e133eb52ee7c55.
All ten recorded native/probe groups close; HOME/TMP are empty. Four synthetic
CLI proofs and two SQLite replays use kernel and JavaScript network denial.
Runtime/source/body/receipt hashes, TLS/request closure and failures are retained.

Final wrapper-fix API replays preserve all three reference Markdown outputs;
two final native CLI checks also preserve Rust/PostgreSQL complete extraction.
These are saved-response checks, not further live requests. Historical100-page
verdicts and broader62 failures/22 missing committed tests remain unchanged.
Actual SafeJS, rendering, credential/passkey/device, TTY and access gates remain.
No buying advice, current software-version claim, speedup or CAPTCHA bypass is
established. The full browser goal remains active; no push is performed.
