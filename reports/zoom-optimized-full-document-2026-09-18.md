# Experimental SDK: unfiltered native Zoom follow-up

September 18, 2026. **The same script still times out; no meeting is joined.**
The experimental scope-sharing candidate does not pass the actual Zoom gate.

## Qualification and live result

The ClrIn4 SDK first passes the unchanged 21-command owned core31 control in
2.117 seconds, including full-policy scripts, native form navigation, fresh
document ownership, stale-reference rejection and insecure-hop denial. Its
self-contained package is sealed at
`/tmp/agent-browser-owned-empty-module-sdk-september18-vWPMbO/package`.
The initial L3V7GC preparation-only assertion incorrectly expected a non-inherited
TypeScript `strict` property; that failure is preserved and launches no child.

A subsequent zero-network full-policy rehearsal passes, followed by exactly one
fresh **unfiltered** live native navigation. It uses the same core31 native build,
runner, document order, CSP handling and limits as the original attempt; only
the qualified SDK package changes. There is no default installation, blocklist,
source rewriting, empty-response substitution, form submission or join action.

| Observation | Original Bq SDK | Experimental ClrIn4 SDK |
| --- | ---: | ---: |
| Successful preceding scripts | 10 | 10 |
| Failed evaluation | 11 | 11 |
| Failed evaluation elapsed | 121,737.279 ms | 120,498.560 ms |
| Configured per-script limit | 120,000 ms | 120,000 ms |
| Whole-process elapsed | 135.558 s | 134.202 s |
| Actual network requests | 13 | 13 |

Both fail on the same 1,497,429-code-unit source, statically classified as
Optimizely experimentation with Amplitude integration. Its SHA-256 is
`4eada9ccde7e7f8140a0db8977c3253e998093456f6f8a5ebaf049f344d8ef1a`.
Whether the join application can omit it remains unknown. Two timed-out runs
are not completed-throughput measurements or proof of a speedup. Fresh document
bytes and inline-script hashes at orders 2, 5 and 7 differ.

The newer loader again reports 47 discovered, 10 executed, 1 failed and 36 skipped
scripts, with `halted: true`. HTTP 200 and a completed native DOM do not establish
successful application initialization. The live run receives 2,127,224 decoded
and 369,842 encoded bytes. Native/SDK owners, all 13 request handles, child and
process group close; retained SDK values and current data are zero. The supervisor
does not time out or send cleanup signals. Private captures remain mode 0600.

## Evidence and next steps

The original unfiltered record remains unchanged. New immutable evidence is at
`/tmp/agent-browser-full-zoom-optimized-september18-O4KRCH/HANDOFF.md`, including
source-by-source comparison, captures, input pins and ownership receipts.

Next steps are distinct: diagnose CPU work versus waiting in a bounded offline
replay, and test the newly explicit origin-blocking option in a separately labeled
filtered navigation. Neither changes the outcome of these unfiltered attempts.
Admission, native incoming audio, permitted recording, transcription and delivery
remain unresolved.
