# W3C tables native flow — September 11, 2026

- **Outcome: document navigation succeeds; genuine same-document click fails.** One fresh public-page native functionality probe, not full specification research, complete styling/rendering or browser conformance certification. No retry or alternate client/transport.
- Released `native-svg-document-integration-september11-round01/snapshot01/dist`, commit `ac70b3a5c42ce676b888a86137e41159387d4330`: parent gate 9614 passed/0 failed/2 explicit existing exclusions, 160 selected/159 strict, 565 manifest entries. All 22 audited source/manifest commit inputs verify; source 1044/compiled 1876 files remain unchanged in place. No mutable source imports, source/build copies, audit/build/test execution or commits.
- Source inventory SHA-256 `9c7de7e5d7e73880b7d98a96487ca66972ede269a398b466041ecf427e2c8207`; compiled `e37c5d5f11d5da5f510aa94db45df93a7a352254781be62e65a571471dae4594`. Before/after ledgers and gate/harness/executable pins verify. Actual audit metadata source `svg-document-work-september11/audit-integration.mjs`, SHA-256 `a76fc68d94e8a232bdb7db8058f3f283208f99cfa8e9b19cfc97bfa43b9b2c94`, is pinned before/after; referenced metadata paths checked at `2026-09-11T19:43:37.331Z` before locking.
- Supervisor UTC `2026-09-11T19:43:37.580Z`–`2026-09-11T19:43:38.290Z`: exit 1, no timeout/output overflow/stream error; child process group 3155179 absent. Exactly 3 native attempts/3 transport requests/3 wire GETs/3 HTTP200 responses/3 wire closes, 0 mocks/redirects, 21,195 encoded and 78,310 decoded bytes. Wire starts are 251ms apart; one navigation/one document commit.
- HTML `https://www.w3.org/TR/CSS22/tables.html`: HTTP200 `text/html; charset=utf-8`, Brotli, native title `Tables`, 2075 DOM nodes; 74,776 decoded/19,594 encoded bytes, SHA-256 `a57bee3e57104001442a313f695fd2e0aff808d9fb56f6e5655a1a3256b9e05e`. Actual Cloudflare status/headers plus bounded native title/content classification show no challenge/access barrier; no Retry-After was observed, and no bypass/retry occurred.
- Original linked CSS both return HTTP200: `https://www.w3.org/TR/CSS22/style/default.css`, 3387 decoded/1448 encoded bytes, SHA-256 `dfc76974e7d607ee89a7feb6a6e443ee57b0ef380d73125ca1ba66f66795b870`; `https://www.w3.org/StyleSheets/TR/2016/W3C-WD.css`, 147 decoded/153 encoded bytes, SHA-256 `bbc3f6c965222686da742407dd46ed3182a03aadc99e958b100add5a87c3c8cd`. Original loader behavior and CSS bytes are retained; no stylesheet stripping or source rewrite.
- **Styling remains partial:** 40 rules/68 declarations/3654 code units/29073 work/2 external sheets. Raw CSS diagnostics: property 9, at-rule 1, unloaded import 1, value 1. Captured `W3C-WD.css` contains `@import "base.css"`; native `css-import-not-loaded` records it, with no import request or harness-origin denial. Its original background-image declaration is retained; no image/background/style-completeness pass is claimed.
- Earlier nonfatal image metadata separately records nine `policy-denied` broken image states and zero image requests. The frozen native loader treats the presence of the observed CSP header as `blockedByCsp`; its image owner reports unsupported CSP enforcement before fetching. This is a native policy/capability boundary, not nine server access denials or a harness origin rejection; no bypass was attempted. These resource diagnostics are not substituted for the exact first thrown flow failure below.
- Native discovery: `body a[href]`, 152 results inspected within cap 512, 52 same-document candidates, one fragment resolution within cap 20. Selected actual anchor `e96`, text `17.5 Visual layout of table contents`, href `tables.html#table-layout`, `_self`, no download/ping; native target `e855` is an existing `a`, with captured markup `<a name="table-layout">`.
- **Exact first thrown flow failure**, stage `native-same-document-link-click`: `AgentBrowserError`, code `unsupported`, message `Document width resolution requires an issue-free supported formatting profile`. Exactly one genuine `BrowserSession.click(e96)`; no fabricated event, direct fragment navigation, scroll fallback, form submission or post-failure interaction.
- After the failed click: same document/root `e1`, URL unchanged without fragment, history `h1-1`/index 0/length 1 unchanged; DOM revision 2078 and native request count 3 unchanged. Existing read-only scroll metrics remain x=0/y=0, revision=-1/builds=0/updates=0: unbuilt metrics, not proof of measured geometry or successful scrolling.
- One bounded read-only native census after the failure: 1946 formatting nodes/2012 boxes/1768 visited DOM nodes/66 outside markers/50614 text units/65267 work; revision 2078 unchanged. **Only active issue category: `display-layout-not-supported: 5`.** All five deferred nodes are DOM `table`, computed display `table`, reason `display-layout-not-supported`: `e752`, `e1036`, `e1444`, `e1506`, `e1634`. Raw `css:` diagnostics remain separate; no partial render is forced past the guard.
- Exact retained caps: one child, native capacity 1, at most 6 bodyless GETs including redirects/subresources, 250ms pacing, 30s+5s, 6MiB output/file, 2,000,000 response/8,000,000 aggregate bytes; style limits 8192 rules/16384 declarations/524288 code units/5,000,000 work, existing DOM/formatting caps. Sole network origin `https://www.w3.org`; credentials forcibly omitted despite upstream native requested-policy metadata, public-address/TLS validation, private HOME/TMP, clean environment, scripts/SafeJS/process/device/TTY denial. Preflight free bytes 2,534,236,160 exceeds 64MiB; lane below 12MiB.
- **42/42 independent evidence checks pass, not flow acceptance.** Final nodes, session tabs/pending loads, queue active/pending, active transport, event listeners/dispatches and image resources/active work are zero/closed; HOME/TMP empty. Fifteen historical lanes/reports and 28 receipt ledgers remain valid, including the prior prelaunch failure and locks; no shared-document/source edits or commits.
- Sealed lane: `node_modules/.cache/native-validation/native-w3c-tables-flow-september11/`. Raw bodies, exact primary failure/progress, stdout/stderr, before/after source/compiled/harness receipts, `VERIFICATION.json`, `RECEIPTS.sha256`, `REPORT-VERIFICATION.json`, `FINAL-RECEIPTS.sha256` and `CHANGED-PATHS.txt` retain this one-shot result. No additional live request or retrospective evidence edit follows.
