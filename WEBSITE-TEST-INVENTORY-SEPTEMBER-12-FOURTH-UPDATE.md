# Website test inventory — September 12, 2026, fourth update

**74 unique recorded attempted hosts, not 74 working websites.** This adds only
`www.netlib.org` to the preceding73-host snapshot. It does not rerun or relabel
the other hosts. The complete normalized host union is in
`reports/website-test-inventory-2026-09-12-fourth-update.json`; the historical
per-host evidence and RFC Editor duplicate correction remain in earlier snapshots.

## Fresh Netlib flow

Native child UTC: September12,2026,06:26:34.198–06:26:34.567. The pinned
12037/addf989 runtime made two real GETs: the homepage and `netlib2.gif`.
Both returned HTTP200, totaling10,631 encoded and10,631 decoded bytes, with
zero mocks, redirects or rejected adapter calls. One document committed:
The Netlib,163nodes,revision164. The native browser inspected21 anchors;
two eligible occurrences resolved to one unique FAQ destination.

One genuine click on the discovered FAQ anchor failed before any destination
request: **document width resolution requires an issue-free supported formatting
profile**. The bounded census shows136 formatting nodes, one deferred
`center` element and no raw or applicable CSS issues. This identifies a
concrete unsupported HTML layout case, not proof that it is the website's only
remaining problem. No access-control or CAPTCHA barrier was observed or claimed.

Sampled document, image, event and control owners closed. Fixed transfer/time
budgets, unchanged native capacities, original source and native identity were
retained; there was no retry, forced navigation or fallback client.

Full sealed report: `NETLIB-DOCUMENTATION-FLOW.md`, SHA256
`49bbd83a22c391852661579a51a5522bf2777d743875d144331392b5da1dac00`.
The parent independently reproduced35 read-only evidence checks,146/148-entry
ledgers and13 actual committed Git blobs under the existing verification
contract. Offline checks deny sockets/socketpair and execute no browser page.

## Outstanding work

The separate12094-case native font release remains isolated acceptance, not
this live flow's runtime. GnuPG non-floating clearance, IANA float/flex/grid/table
coordination and Netlib's deferred center layout remain observed failures.
Historical reports, working-site acceptance, research completeness and real
provider/credential/passkey-device/TTY/SafeJS/challenge gates remain unchanged.
