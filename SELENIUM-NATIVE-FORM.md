# Selenium native public form — September 11, 2026
- Outcome: **NOT ACCEPTED — harness admission boundary**, not a website/browser bug or styling pass; one live attempt, no retry, no CSS omission or modification.
- UTC supervisor start/end: `2026-09-11T14:58:09.409Z` / `2026-09-11T14:58:09.651Z`; native child: `2026-09-11T14:58:09.497Z` / `2026-09-11T14:58:09.645Z`; exit `1`, no timeout.
- Pinned existing `node_modules/.cache/native-validation/native-grid-list-integration-september11-round01/snapshot01/dist`; historical 8,183 native passes, one exclusion, zero failures; no new suite run or duplicated snapshot.
- Source inventory SHA-256: `ce0c2d1db605991642f7032626c5f64fa7d107c74d35eb83e9542ff36e84ed9f` (1,017 files); compiled inventory: `f9c9868a292cba958f1be44f04de68b2c50be2d906d35c1c0b98d3000789fb26` (1,812 files), inventoried before this run; both unchanged afterward.
- First failure: stage `initial-navigation:network`, `AssertionError`, code `ERR_ASSERTION`; exact JSON-escaped message: `"The expression evaluated to a falsy value:\n\n  assert(origins.includes(requestedUrl.origin))\n"`. The later `Navigation aborted` is secondary.
- Original markup requested `https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css`; the same-origin-only harness denied it before transport. Original CSS behavior remains intact; widening admission requires separate parent authorization.
- Accounting: two native GET attempts, one native-transport request, one HTTPS request call/reply/close, zero redirect responses/follows, zero cross-origin wire requests; only `https://www.selenium.dev/selenium/web/web-form.html` reached the wire (HTTP 200).
- Response evidence: `text/html; charset=utf-8`, gzip, 1,233 encoded / 4,988 original transport-decoded bytes; body SHA-256 `fbf64bd0731a21f7e77abfde1b3bfc01377215783952facaccc8ed5eb105c6c3`; bounded body and sanitized metadata retained before loading.
- Captured-markup-only observations: `<form method="get" action="submitted-form.html">`, text `my-text` / `my-text-id`, select `my-select`, checkboxes `my-check`, submit button. No committed native page, native control selection, fill/select/check, submit click, or destination content was reached; this is not form-flow acceptance.
- Bounds: one tab, two navigations maximum, 12 requests, 2 MB/response, 8 MB aggregate network bytes, 30 s deadline + 5 s termination grace, 6 MiB output/file cap; omitted credentials; no page scripts/SafeJS, alternative browser, external fetch, TTY, uploads, accounts, or authenticators.
- Cleanup: immediate 179 document nodes / one pending load settled to all tracked documents closed / zero pending loads in 49.652358 ms; zero tabs/active requests, closed transport/session, empty private home/tmp, process group absent. Original verifier retains 24/25 checks: its sole failure incorrectly tests immediate nodes as final; additive offline `REVIEW.json` verifies final cleanup without changing original evidence.
- Evidence lane: `node_modules/.cache/native-validation/native-selenium-form-flow-september11/`; exact changed-file list: `node_modules/.cache/native-validation/native-selenium-form-flow-september11/CHANGED-PATHS.txt`; initial and additive ledgers are `RECEIPTS.sha256` and `REVIEW-RECEIPTS.sha256` within that lane.
- Changed scope: only the new evidence lane and `SELENIUM-NATIVE-FORM.md`; source/tests/TASKS/manifest/prior lanes untouched by this worker, no commit or push. Offline syntax and guarded pinned-import checks passed; a later wider-origin live run remains unauthorized here.
