# OpenBSD native documentation flow — September 11, 2026

- **Initial FAQ commits, but the single genuine documentation-link click fails at the native width guard.** The committed HTTP200 document is `https://www.openbsd.org/faq/index.html`, native title `OpenBSD Frequently Asked Questions`,668 DOM nodes. Native text contains the FAQ introduction, quick links, introduction and installation sections. This is a bounded live interaction failure, not a website access denial or whole-site acceptance.
- Runtime is exclusively the in-place `native-stylesheet-import-integration-september11-round01/snapshot01/dist`, release `e82ccb3199c60bbb5d4683321d66c88b263e1ccd`. Exact gate base from `EXPECTED.json` is `e9929a553351cc4b8085a47df71b9307444c847a`. Preflight verifies gate-local AUDIT/audit source, all20 receipts, COMMIT-VERIFICATION and8 owned source/test inputs plus the manifest against commit:9 verified inputs,1064 source/1908 compiled files. No current dirty source or copied/rebuilt runtime executes.
- **Historical gate only:**10335 passes/0 failures/2 existing exclusions,173 selected/172 strict,577 manifest entries; UTC `2026-09-11T21:39:20.369Z`–`2026-09-11T21:41:39.092Z`. Eight syntax-only harness checks and corrected literal `snapshot01/` prefix assertions/normalization pass before GO. No historical build/test/strict/format/audit gate is rerun.
- Source ledger SHA-256 `4d5d1bea5c0346fce9aa87a442d94524fed0385103614b428db3b95f5d3633b6`; compiled `32ab79439722ef329c59ab5c60ffa9172b8fd44f369ddbb0a3387db0e3280232`; AUDIT `e4c10844fc2153f95710134da95ec89073fcf985b723e55041b47ea04ccc9cb2`; actual gate-local audit source `0856556cbd2adf0ab931253162659be343ebaa4a5fef1a04a0665eae94b809bc`. Source/dist inventories, gate inputs, harness and security-executable pins remain unchanged before/after.
- Exactly one GO at `2026-09-11T21:55:23.849Z`; one child supervised `2026-09-11T21:55:23.850Z`–`2026-09-11T21:55:24.571Z`. Native report interval `2026-09-11T21:55:23.965Z`–`2026-09-11T21:55:24.562Z`. Child exits1; process group3279209 is absent;34835 output bytes,0 stderr, no timeout/signal/spawn/stream error or output overflow.
- Exact accounting:2 adapter entries,2 admitted requests,0 rejected adapter entries,2 request-start records,2 transport requests,2 wire requests/responses/closes,0 mocks,0 redirects. Both responses are HTTP200, with15706 encoded and15706 decoded bytes total,250ms observed spacing. One navigation call/one document commit/one click; no clicked-destination request. Status/header and committed-content classifiers find no access barrier; no Retry-After is observed.
- Original HTML is10381 bytes, SHA-256 `86289bab025f4a0f26d900a3b5c0f11903d2c6e429558e971c536522ee862e0b`. Original `/openbsd.css` is5325 bytes, SHA-256 `25cd5cb07473f5b1be143e611df5332c0532c9cd4db0987d76896da0426ae80b`. Both unchanged transport-decoded bodies, URL/status/header metadata and hashes are retained; no HTML/CSS/resource is omitted or rewritten to obtain a page.
- Native discovery inspects138 `body a[href]` results, finds128 matching same-origin FAQ/document candidates, and selects actual anchor `e98`, text `Introduction to OpenBSD`, href `faq1.html`, resolved target `https://www.openbsd.org/faq/faq1.html`, target `_self`, no download or ping. `BrowserSession.click` is attempted exactly once. At `2026-09-11T21:55:24.557Z`, stage `native-documentation-link-click` fails with `AgentBrowserError`, code `unsupported`, message `Document width resolution requires an issue-free supported formatting profile`.
- Before/after failed-click state remains initial URL, root `e1`, revision668, history key `h1-1`, index0/length1. Scroll metadata remains unbuilt, not successful geometry. No direct-navigation fallback, synthetic dispatch, forced geometry, retry or alternate-client request occurs; the destination remains unvalidated.
- One bounded read-only post-failure formatting census separates non-CSS active `display-layout-not-supported:1` from raw CSS `css:unimplemented-css-property:8`, `css:unimplemented-or-invalid-css-value:1`, `css:unimplemented-or-invalid-media-query:13`. The first two raw CSS categories remain non-advisory under the released classifier. Exactly1 deferred ref is sampled: `e44`, table/display-table, reason `display-layout-not-supported`; no claim is made that this table alone caused the full-profile rejection.
- Census metrics:691 formatting nodes,806 boxes including115 outside markers,653 visited DOM nodes,3948 text units,10040 work; document revision stays668. Native styles retain1 external sheet,0 imports,84 rules,136 declarations,5350 code units and8972 work. This diagnostic tree is not accepted page geometry or partial-rendering fallback. No image element/request is reported by the instrumented native image owner.
- **Observed owner cleanup is proved, not inferred from empty arrays:**1 instrumented event owner is closed with0 listeners/active dispatches;1 instrumented image owner is closed with0 resources/active/queued/waiters. Document nodes0, tabs0, pendingLoads0, cleanupErrors0, active transport0, queue active/pending0; session/transport/queue are closed. Both wire requests close, HOME/TMP are empty, and no settlement wait is needed. Proof covers these instrumented owners only.
- Caps remain one child/nativecapacity1,8 bodyless GETs including redirects/subresources,250ms pacing,30s+5s grace,6MiB output/file and unchanged response/aggregate/style/DOM budgets. Private environment allowlist, native public-address/TLS/origin guards and seccomp remain enforced. The adapter forces credentials `omit` despite incoming loader preference; wire headers contain no credentials and the jar accepts0 cookies. No scripts/SafeJS, providers, TTY/device, alternate browser/search/fetch/curl, identity masking, challenge solving or access-control bypass occurs.
- **48/48 evidence checks pass, not the clicked website flow.** Preflight has1494982656 bytes free; evidence seal has1753088 allocated lane bytes and1489063936 bytes free, within12MiB lane/64MiB free limits. Original GNU follow-up47-file/report preservation ledger and both GNU, SQLite and W3C evidence sets/eight historical receipt ledgers verify unchanged.
- New evidence is confined to `node_modules/.cache/native-validation/native-openbsd-flow-september11/` and this report. Named checks, original bodies, source/dist/harness pins and report are sealed by `RECEIPTS.sha256` and `FINAL-RECEIPTS.sha256`; run/launch locks remain retained. No production/test/shared-doc/manifest edits, builds or commits occur. Parent retains responsibility for new-host inventory; this lane does not alter counts or claim whole-site acceptance.
