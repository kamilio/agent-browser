# Website inventory: September 14 twenty-seventh update

This append-only update adds **one fresh live attempt on one previously tested
host, zero new hosts and eight real HTTPS requests**. It supplements the prior
twenty-sixth captured update without changing any historical measurements.

| Host | September 14 native UTC | Runtime | Actual result |
| --- | --- | --- | --- |
| `man7.org` | 15:47:57.021–15:48:00.348 | `71d0c8b59f7e92532eb956a972c3cb118789e9b7` | **Verified live ls(1) → genuinely discovered date(1) flow; two native document commits.** |

The browser loads ls(1), discovers and ordinarily clicks date(1), then loads and
interprets the destination: title `date(1) - Linux manual page`, 531 nodes and
9,897 body-text code units. The initial document is released. This is a successful
specific live workflow, not general man7 or full-page rendering acceptance.

Eight HTTP 200 responses cover two HTML documents and three shared resources
requested for each page, totaling **46,184 encoded / 78,042 decoded bytes**.
There are zero redirects, mocks or retries. Ten adapter entries include two
optional tracker denials before transport; the tracker is not a visited host.
No scripts, credentials, providers/passkeys, devices, SafeJS, submissions,
fingerprint spoofing, CAPTCHA bypass, forced click or alternate browser is used.

Child/supervisor exit 0. All **14 independent evidence checks pass**, followed by
a successful seal recheck. No challenge or HTTP restriction is encountered, so
live challenge handoff remains unverified. Native owners and process group close
cleanly; private directories remain empty. No performance comparison is claimed.

The preceding captured recheck genuinely stopped at missing date(1) data. This
fresh flow obtains that destination in a **new complete eight-response corpus**;
it does not repair or relabel the old archive. Parent preparation corrections
occurred before any browser launch and do not count as extra site attempts.

Report: `MAN7-LIVE-FLOW-SEPTEMBER-14.md`.
Evidence: `/dev/shm/agent-browser-man7-live-flow-september14/`, with independent
parent closeout and byte-verified durable copies under
`node_modules/.cache/native-validation/man7-live-flow-september14/`.

Next use the complete corpus for regressions and move to other hosts' blockers,
including HN text/background support. Broader live coverage, performance tests,
the requested research, providers/passkeys, devices and challenge gates remain
outstanding. The overall goal stays active; no new host is counted here.
