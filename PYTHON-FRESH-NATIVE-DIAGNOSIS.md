# Python fresh-corpus native formatting diagnosis

## Result

The corrected parent-owned offline replay reaches native document commit and
formatting, then its single used-layout attempt rejects with `unsupported`:
`Float formatting ownership does not match its diagnostics`.

This result uses the exact eight newly captured Python responses on committed
native15522 (`43060222e28db7abb3259b0ad50d230f359705c6`). It is not a live retry,
successful geometry, interaction, or whole-site acceptance. No response, style,
DOM attribute, image error, or resource limit was changed to admit the document.

The native interval is September12,2026,19:37:34.473–19:37:34.612 UTC. The parent
supervisor runs19:37:34.314–19:37:34.624, exit0, no stderr or timeout. Exit0 records
a completed diagnostic and cleanup, not successful layout.

## Distinct retained attempts

- The earlier historical seven-response replay could not supply `basic.css`.
  Its original corpus and failure remain in `PYTHON-MODERN-REPLAY.md`.
- The fresh live load captured eight HTTP200 responses, including basic.css,
  then its conservative harness stopped on unsupported py.svg decoding before
  commit. `PYTHON-FRESH-NATIVE-INITIAL.md` preserves that separate gate.
- The first new worker memory replay failed19:34:32.329 before commit because
  its harness compared null-prototype native headers with a plain JSON object.
  Native transport had already served one19434byte response; the worker's
  post-assertion capture count of zero does not mean zero native responses.
  Its separate `native-python-current-replay-september12` evidence is retained.
- This parent-owned lane uses the already-proven header-entry comparison and
  exact-URL response map, without changing header values or imposing artificial
  network response ordering. It does not rerun or edit the failed worker lane.

In this separately authorized offline scope, the unchanged native loader may
represent only the three already-observed unsupported py.svg states. Any other
image error, global image failure, policy/resource error, or missing response
would stop the diagnostic. No error is cleared and no SVG pixels are invented.

## Measured native observations

Page title is `3.14.7 Documentation`, with853nodes and revision860 after loading.
The revision remains unchanged during the subsequent diagnostic. Eight exact
memory responses serve72064decoded bytes; there are zero wire requests/bytes,
zero clicks, one load, one committed document, one formatting pass and one
used-layout attempt. Source declarations remain intact.

The native style owner reports579rules,1071declarations, five external sheets
and one imported sheet. Raw issues are93unsupported properties,
22unsupported/invalid values and six unsupported/invalid selectors. Applicable
counts are22properties, six values and six selectors. The formatting result is:

| Diagnostic | Count |
| --- | ---: |
| Unsupported/invalid CSS selector | 6 |
| Unsupported CSS property | 22 |
| Unsupported/invalid CSS value | 6 |
| Float layout | 10 |
| Inline vertical alignment | 2 |
| Deferred display marker | 9 |
| Position layout | 1 |
| Overflow layout | 1 |
| Clear layout | 1 |

The nine deferred display nodes are two inline-flex switchers, three flex
containers and four grid content tables. As with other native decomposition
markers, their presence alone does not prove that the corresponding coordinator
cannot handle an otherwise supported page. The sole recorded overflow group is
`div.sphinxsidebarwrapper`, block display, hidden horizontal and auto vertical
overflow. No legacy HTML hint group or unsupported image-element diagnostic is
present in this formatting pass; this does not demonstrate successful image
painting or full-page rasterization.

Image references e103/e283/e759 remain complete/broken with `unsupported` for
the same py.svg URL, zero natural sizes, one shared resource, one image fetch,
2041received bytes and zero decoded pixels. Global image failure is absent.
The exact native coordinator rejection is now visible without conflating an
ordinary unsupported image state with exhausted capacity.

## Evidence and next work

Lane:
`node_modules/.cache/native-validation/html-cell-spacing-work-september12/python-diagnostic00/`.
The parent task is `PYTHON-DIAGNOSTIC.md`; the lane retains its exact contract,
probe, runner, RESULT, stdout/stderr and supervisor summary. The input is the
separately verified fresh280-entry capture, not the older seven-response corpus.
Capture ledger SHA256:
`979bd3d6d854abfdb5ed4d290fc2cb304c6a101ec97a722ae18143de7b46ead2`.

The kernel-network-denied child uses the unchanged native15522 release,
12request/1concurrent and original response/work limits,30second supervisor,
6MiB supervisor output,32KiB result and64combined diagnostic-sample caps.
Before/after checks retain source, compiled, gate and capture identities.
Document/image/session/transport/queue cleanup is verified with zero nodes,
pending loads and active work. This smaller parent harness does not independently
instrument event/control-owner cleanup and makes no such claim. No scripts,
SafeJS, credentials/providers, devices, TTY or socket self-probe run.

`verify-python-diagnostic.mjs` performs readonly result/capture/release checks
and records a new receipt ledger; it does not parse or replay the page again.
The next targeted investigation is float diagnostic applicability/ownership,
followed by the independent CSS and layout issues. No float fix, complete Python
layout or live navigation acceptance is claimed by this report.
