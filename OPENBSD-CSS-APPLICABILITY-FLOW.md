# OpenBSD CSS applicability follow-up — September 11, 2026

- **Fresh initial FAQ commits with byte-identical baseline HTML/CSS, but the single genuine Introduction to OpenBSD click still fails.** This is an actual native live result, not a mock/replay, website access denial, successful interaction or whole-site acceptance. The earlier preparation-only status/report remain preserved in the private lane as historical preparation evidence.
- Only the in-place `native-css-diagnostic-applicability-september11-round01/snapshot01/dist` executes: release `11f31bfd279b60520d07bafb0cd929884b384257`, exact audit base `9b38bec02a06688f728a147ce75309e9ab112087`. Parent GO is copied exactly and pinned at SHA-256 `174355d1c1e51728d777bff29bee4583d90d501efae2c1ccb6882c171f15847f`. Nine syntax checks and corrected preflight pass; all20 gate receipts, gate-local AUDIT/audit source and COMMIT-VERIFICATION match supplied pins, and5 owned source/test inputs plus manifest match commit:6 inputs,1066 source/1908 compiled files. No old-runtime/current-source substitution or build occurs.
- **Historical gate only:**10390 passes/0 failures/2 existing exclusions,175 selected/174 strict,579 manifest entries; original UTC `2026-09-11T22:06:22.549Z`–`2026-09-11T22:08:41.421Z`. No historical build, test, strict, format or audit gate is rerun. Source ledger SHA-256 `0b49489e38cc1a7e0c58f419d61e2305f4fe2932969bfe1d796c10906c153f50`; compiled `081950b7daa86aee4e27b28a9084b71cdd176c01e90112b5db2c56589cf11b42`; AUDIT `b0937900ecdb4f0df79834041644a7b3691b67088661f1ee75a7206d0dc6390c`; actual audit source `1d1c52f03340a431ba71691c37befafb48d3aae91811acebabe135ecd3c5e926`. All runtime/source/harness/security pins remain unchanged before/after.
- Exactly one GO at `2026-09-11T22:12:57.302Z`; one child supervised `2026-09-11T22:12:57.303Z`–`2026-09-11T22:12:57.952Z`. Native report UTC `2026-09-11T22:12:57.438Z`–`2026-09-11T22:12:57.942Z`. Child exits1, process group3296460 is absent;38542 output bytes,0 stderr, no timeout/signal/spawn/stream error or overflow.
- Actual committed page: `https://www.openbsd.org/faq/index.html`, HTTP200, native title `OpenBSD Frequently Asked Questions`,668 DOM nodes, root `e1`, revision668. Native content includes the FAQ introduction, quick links and Introduction to OpenBSD section. Discovery inspects138 genuine body anchors, finds128 same-origin FAQ candidates and selects actual `e98`, text `Introduction to OpenBSD`, href `faq1.html`, target `https://www.openbsd.org/faq/faq1.html`, target context `_self`, no download/ping.
- Exactly one `BrowserSession.click` fails at `2026-09-11T22:12:57.936Z`, stage `native-documentation-link-click`, `AgentBrowserError`, code `unsupported`, message `Document width resolution requires an issue-free supported formatting profile`. Current URL/title/root/revision stay unchanged; history remains `h1-1`, index0/length1. Scroll metadata stays unbuilt, not accepted geometry. One navigation call/one commit; destination document is not requested. No substitute anchor, fallback navigation, synthetic dispatch, forced geometry or retry occurs.
- Exact accounting:2 adapter entries,2 admitted requests,0 rejected adapter entries,2 request-start records,2 transport requests,2 wire requests/responses/closes,0 mocks and0 redirects. Both GETs return HTTP200;15706 encoded and15706 decoded bytes,250ms spacing. Response/header and committed-content classifiers find no access barrier; no Retry-After is observed.
- **Actual initial-body parity is identical:** fresh HTML10381 bytes, SHA-256 `86289bab025f4a0f26d900a3b5c0f11903d2c6e429558e971c536522ee862e0b`; fresh `/openbsd.css`5325 bytes, SHA-256 `25cd5cb07473f5b1be143e611df5332c0532c9cd4db0987d76896da0426ae80b`. Both match the baseline exactly. Originals, status/header metadata, URLs and hashes are retained, not replayed, rewritten or omitted. Matching these two inputs alone does not establish engine-only causation; the prepared changed/missing-input caveat remains explicit.
- **Raw CSS**, sourced only from `styles.metrics().issues`: `unimplemented-css-property:8`, `unimplemented-or-invalid-css-value:1`, `unimplemented-or-invalid-media-query:15`. Historical baseline raw media-query count remains13; fresh raw count is15 despite identical body bytes. Both original measurements are retained without rewriting evidence or calling this a content change.
- **Applicable CSS**, independently sourced from `styles.metrics().applicableIssues`: `unimplemented-css-property:1`, `unimplemented-or-invalid-media-query:15`. **Filtered formatting CSS** contains the corresponding `css:`-prefixed entries, not the full raw set; its non-advisory CSS count is `css:unimplemented-css-property:1`. **Non-CSS formatting** independently retains `display-layout-not-supported:1`. No filtered metric is relabeled raw CSS, no diagnostic is suppressed, and the residual table entry is not asserted to be the sole cause of full-profile rejection.
- One bounded read-only post-failure census samples1 deferred ref, `e44`, table/display-table, reason `display-layout-not-supported`; the20-ref cap remains unchanged. Metrics:691 formatting nodes,806 boxes including115 outside markers,653 visited DOM nodes,3948 text units,10040 work. Revision stays668. This is diagnostic evidence only, not successful geometry or partial-rendering fallback.
- **Cleanup is proved for actual instrumented owners:**1 event owner closed with0 listeners/active dispatches;1 image owner closed with0 resources/active/queued/waiters. Document nodes0, tabs0, pendingLoads0, cleanupErrors0, active transport0, queue active/pending0; session/transport/queue are closed, both wire requests close, HOME/TMP empty. No settlement wait is needed; no owner proof is inferred from empty arrays.
- Unchanged bounds: one child/nativecapacity1,8 bodyless GETs including redirects/resources,250ms pacing,30s+5s,6MiB output/file, existing response/aggregate/style/DOM budgets, private allowlisted environment, native public-address/TLS/origin guards and seccomp. Credentials are forced omitted; no credential headers or accepted cookies. No scripts/SafeJS, alternate clients, providers, device/TTY, identity masking, challenge solving or access-control bypass. Preflight has1415593984 bytes free; evidence seal has1806336 allocated lane bytes and1381875712 free, satisfying64MiB free/12MiB lane gates.
- **50/50 named evidence checks pass, not the native interaction.** Original OpenBSD baseline40-file/report preservation ledger and all five prior evidence sets/ten historical receipt ledgers verify unchanged. Both `RECEIPTS.sha256` and `FINAL-RECEIPTS.sha256` seal original bodies, actual parity, distinct CSS diagnostics, runtime/source/harness pins, checks and this report. Run/launch locks remain retained; no relaunch or cap change occurs.
- Work is confined to this report and `node_modules/.cache/native-validation/native-openbsd-css-applicability-flow-september11/`. No production/test/shared-doc/manifest edits, builds or commits. Parent retains host-inventory responsibility; no counts or working-page/whole-site acceptance claims are added.
