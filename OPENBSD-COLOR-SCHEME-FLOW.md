# OpenBSD native default color-scheme flow — September 11, 2026

- **Primary outcome: the genuine Introduction to OpenBSD click succeeds and commits the destination.** Both initial original HTML/CSS comparisons match the two previous fresh captures. This validates the bounded default-mode FAQ-to-introduction flow, not whole-site compatibility or a second destination interaction. The primary result was reported before final ledger work; preparation-only records remain preserved as historical preparation evidence.
- Runtime is exclusively the in-place `native-color-scheme-september11-round01/snapshot01/dist`, release `8936f29698f8f97d7ec4ac309d7459c6c2764539`, audit base `5269b34ddc5e26231b1b5582abaf28f092dd6099`. Nine syntax checks and preflight pass. All20 gate receipts, gate-local AUDIT/audit source and COMMIT-VERIFICATION match parent pins;12 owned source/test files plus manifest match commit:13 inputs,1071 source/1912 compiled files. Parent GO is preserved verbatim in `PARENT-GO.md`; its owned authorization receipt adds only the exact commit-binding line and has SHA-256 `93740ae5d6bc0e599796a0394f082546066b5fa6653249e22327e54657c4c1eb`.
- **Historical gate only:**10715 passes/0 failures/2 existing exclusions,182 selected/181 strict,583 manifest entries, UTC `2026-09-11T22:48:42.919Z`–`2026-09-11T22:50:59.687Z`. No historical build/test/strict/format/audit gate is rerun. Source ledger SHA-256 `a12edb1ba7c97500073c079fc10c9819f74bab9805804412a0031cb8e5a4f820`; compiled `3f768aea19e0bdac81141f298846b8c15a7be147930288ecce0d8639d73dc925`; AUDIT `89fcb8f1640be5da7787180033f7f1873ab2cf07c31da7624ec6e48a968be563`; audit source `d52ed4961c23ae60a5de1444b572506894a09befd2f8359a7d42e441ca01a6d7`. Source/dist, gate, harness and security-executable pins remain unchanged before/after.
- Exactly one GO and child, supervised UTC `2026-09-11T22:54:33.461Z`–`2026-09-11T22:54:34.803Z`; native report interval `2026-09-11T22:54:33.573Z`–`2026-09-11T22:54:34.748Z`. Exit0, no first failure, process group3328678 absent;101816 output bytes,0 stderr, no timeout, signal, spawn/stream error or output overflow.
- Initial HTTP200 document `https://www.openbsd.org/faq/index.html` commits with native title `OpenBSD Frequently Asked Questions`, root `e1`,668 nodes/revision668. Fresh native discovery identifies the actual `e98` body anchor, text `Introduction to OpenBSD`, href `faq1.html`, resolved URL `https://www.openbsd.org/faq/faq1.html`, target `_self`, no download/ping. The historical ref is not forced and no destination navigation is fabricated.
- Exactly one `BrowserSession.click` produces the native mouse/action result at234,213, with default action not prevented and a document navigation. Destination HTTP200 commits as `OpenBSD FAQ: Introduction to OpenBSD`, root `e669`,574 nodes/revision574; session history advances from index0/length1 to index1/length2. Native destination content includes About OpenBSD, Hardware Support, Manual Pages, Mailing Lists and project introduction text. Old document nodes are0. There is one explicit initial navigation plus one click-generated navigation,2 commits, no direct fallback and no second click.
- **Actual read-only preference evidence:** four page-state samples across the initial and destination documents report `preference:null`, `effective:light`, `profile:native-ua-color-preference`, `systemIntegration:false`, `siteOverrides:false` from `page.styles.metrics().colorScheme`. `session.colorSchemePreference(tabId)` independently returns null in each sample. These are observed values, not merely design labels. No setter, constructor preference override, dark second run, OS detection request or site override is used.
- Exact accounting:4 adapter entries,4 admitted requests,0 rejected adapter entries,4 emitted request-start records,4 transport requests,4 wire requests/responses/closes,0 mocks and0 redirects. All four bodyless GETs return HTTP200 from the sole allowed origin `https://www.openbsd.org`: initial HTML/CSS, then destination HTML/CSS.38561 encoded and38561 decoded bytes; observed spacing250,392,251ms. Response/header and both committed-content classifiers find no access barrier; no Retry-After is observed.
- **Two independent initial-body comparisons are identical**, against both `native-openbsd-flow-september11` and `native-openbsd-css-applicability-flow-september11`. Fresh initial HTML10381 bytes has SHA-256 `86289bab025f4a0f26d900a3b5c0f11903d2c6e429558e971c536522ee862e0b`; CSS5325 bytes has `25cd5cb07473f5b1be143e611df5332c0532c9cd4db0987d76896da0426ae80b`. Destination HTML17530 bytes has `35dc4df9a0665128437093852c42fa149ef66be9f8150ff6dae65b3b29da5f43`; its separately fetched CSS has the same CSS hash. All four actual originals and metadata are preserved. These are comparisons of new GETs, not replay or resource substitution; matching inputs alone does not prove engine-only causation.
- Raw `styles.metrics().issues` is retained separately on both pages: `unimplemented-css-property:8`, `unimplemented-or-invalid-css-value:1`, with no media-query issue entry. Initial `styles.metrics().applicableIssues` is empty. Destination applicable metrics retain `unimplemented-css-property:1`. **The destination residual is reported, not treated as whole-site compatibility:** no second destination interaction or geometry-acceptance probe is performed, so its actionability under that diagnostic remains unvalidated. No post-failed-click formatting census runs because the authorized click succeeds; filtered formatting entries are not relabeled raw CSS.
- **Cleanup is proved for actual sampled owners:**2 event owners close with0 listeners/active dispatches;2 image owners close with0 resources/active/queued/waiters. Both documents have0 nodes; session tabs0, pendingLoads0, cleanupErrors0, active transport0, queue active/pending0; session/transport/queue are closed. All4 wire requests close, private HOME/TMP remain empty, and no settlement wait is needed. No proof is inferred from empty owner arrays.
- Every execution bound remains: one child/nativecapacity1,8 bodyless GET maximum including redirects/resources,250ms pacing,30s+5s grace,6MiB output/file and original response/aggregate/style/DOM budgets; private0700 lane/HOME/TMP, allowlisted environment, public-address/TLS/origin guards and seccomp. Credentials are omitted, with no credential headers or accepted cookies. No pruning, raised caps, retry, alternate client, provider, page scripts/SafeJS, TTY/device, identity masking, CAPTCHA solving or bypass. Genuine challenge handling remains stop-and-handoff, never automatic retry.
- **51/51 named evidence checks pass, including the bounded native interaction and observed-state checks.** Preflight free space1582878720 bytes; evidence seal lane allocation1970176 bytes with1581076480 bytes free, within64MiB free/12MiB lane limits. Both previous OpenBSD captures/reports (85-file preservation ledger) and all six historical evidence sets/twelve prior receipt ledgers verify unchanged. Source/dist, originals, dual parity, raw/applicable diagnostics, state, owner cleanup and this report are sealed by both `RECEIPTS.sha256` and `FINAL-RECEIPTS.sha256`; run/launch locks remain retained.
- Work is confined to this report and `node_modules/.cache/native-validation/native-openbsd-color-scheme-flow-september11/`. No production/test/shared-doc/manifest edits, builds or commits. Parent retains host-inventory and broader acceptance responsibility; this result does not change counts or claim all-site support.
