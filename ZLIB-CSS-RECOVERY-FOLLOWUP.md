# Parent verification of zlib CSS recovery replay

The parent independently reruns the read-only verifier on September12,2026.
All eight actual local Git outputs and17 socket-denied assertion groups pass;
the final ledger remains unchanged. This launches no browser, performs no HTTP,
and does not reopen the closed one-replay allowance.

The actual new native run is September12,2026,05:29:46.665–05:29:48.729 UTC on
61ca986/11952, using nine exact captured responses as mocks. One document commits
and one genuine discovered FAQ click fails before any destination request.
The original11784 live capture window and51535 encoded bytes remain historical;
this new replay has zero wire bytes and74133 decoded mocked response bytes.

The old invalid CSS-rule diagnostic drops1→0. One advisory discarded-rule
diagnostic replaces it, with original style bytes preserved. Center, table and
HTML presentation guards remain, so this confirms the bounded recovery feature,
not a working zlib FAQ flow or a fresh live revisit. It adds no host to the
72-host attempted inventory and does not change any earlier outcome.

Full sealed evidence: `ZLIB-CSS-RECOVERY-REPLAY.md`.
Report SHA256:
`cd152d17dfe2c5d96f3d36bfb60327e6d4cb9df3a3ff3e6d642af78c6b63e035`.
Final ledger SHA256:
`fc67bb0fd20cfe24c4370ed565d2b54457317c7d8e84e6beb8caf29301e2e8af`.
Parent read-only verification output:
`node_modules/.cache/native-validation/float-atomic-work-september12/parent-zlib-verification`.

The complete run contract, raw/applicable/non-CSS census, current-DOM discovery,
exact source attribution, sampled cleanup and artifact membership are retained
in the sealed report and lane. Independent CSS/layout, website, provider/device
and challenge-handling acceptance remain open.
