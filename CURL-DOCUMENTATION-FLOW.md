# curl documentation: single native flow

- Outcome: **NATIVE ACTIONABILITY REJECTION**, not an observed website-access rejection and not proof of an engine defect. The one selected FAQ click failed with `not-actionable`: “Click target is hidden, inert or disabled”. No retry, second click, alternative target or destination request occurred.
- Sole child: 2026-09-11T23:56:42.146Z–2026-09-11T23:56:42.577Z, exit1, process group3391795 absent; output63941 bytes, stderr0, no timeout/signal/overflow/stream error. Native report spans2026-09-11T23:56:42.259Z–2026-09-11T23:56:42.568Z.
- Nine syntax checks and exact preflight passed before GO. Audited runtime `4d11abddf8f9a743a0066ef242a7fa07ae297e71`: historical11025 passed/0 failed/2 excluded,189 selected/188 strict,586 manifest entries,13 owned+manifest14, actual1075 source/1916 compiled,20 release receipts. No gate rerun, build, current-source substitution or in-progress positioned-float code was used. Only the curl task authorized these URLs; the font GO supplied runtime pins.
- Actual attempted host: **curl.se**, proven by the native wire request at2026-09-11T23:56:42.267Z and HTTP200 replies for `/docs/` and `/curl.css`. The FAQ URL was discovered but never fetched. Parent-reported65 prior attempted hosts was authorization context only; no shared host inventory or automatic global total was changed, and host attempt is not flow success.
- Accounting:2 adapter entries/admissions/bodyless native GETs/transport requests/wire calls,2 HTTP200 responses,2 closes,0 mocks/rejections/redirects;6556 encoded bytes /28849 decoded bytes. Request starts251ms apart. No Retry-After, CAPTCHA or classified access restriction was observed; guard attempts0.
- Fresh originals: HTML8507 bytes SHA256 `e7ae0135882c0911eb952a7ccfeb7119899ceeee654ab68f5a4868d315bf0072`; CSS20342 bytes SHA256 `9f9c161119f6fcb317548bca855456247cc9c2df2ae0c28042a993be279cd1c8`. Bodies are retained unchanged as `response-1.body` and `response-2.body`, with response metadata and loader provenance; no parser fix, resource rewrite or alternate fetch.
- Initial native page: `curl - Documentation Overview`, root`e1`,541 nodes/revision543, history`h1-1` index0/length1. Discovery returned98 native body anchors and inspected only the first96:76 qualifying occurrences,41 unique destination URLs,35 duplicates. Destination deduplication retained each first native reference; the preferred FAQ URL appeared twice, and its first observed ref`e82` was selected, href`/docs/faq.html`, target_self.
- Exact failure stage `native-documentation-link-click`, `AgentBrowserError`, code`not-actionable`; clickCalls1, no returned click result, mouse actions0. URL/document/root/revision/history/scroll/request count remain unchanged. The generic native error does not establish which hidden/inert/disabled condition applies; no further actionability probe or substitute visible link was attempted.
- Raw CSS metrics: invalid/unimplemented value26, unimplemented property53, invalid/unimplemented selector6, invalid/unimplemented media query5. Applicable CSS separately: value5, property19, selector6, media query5; filtered formatting CSS carries those same applicable counts with `css:` prefixes. Media-query entries are advisory; value/property/selector entries are non-advisory. Original CSS loaded as1 external sheet,172 rules,487 declarations,20342 code units.
- Independent non-CSS census: `overflow-layout-not-supported:1`, `positioned-layout-requires-coordination:1`, `display-layout-not-supported:9`, `element-layout-not-supported:1`. Ten deferred subtrees:9 flex-display nodes and1 image element.320 visited DOM nodes,334 boxes,327 formatting nodes,7 outside markers,1689 text units,3516 work; revision543 unchanged. This post-failure census does not establish the immediate click cause.
- Original native image owner reported `/logo/curl-white-symbol.svg` ref`e30` broken with `policy-denied`,0 image requests and0 loaded image resources. This is recorded resource-policy state before any image wire request, not a server rejection or harness pruning; it was not bypassed.
- Cleanup: one actual document, event owner, image owner and file-control/mouse owner instrumented. Retained nodes/text/inline declarations/collectors/files/bytes/buttons/pressed/listeners/dispatches/image resources/active/queued/waiters are zero and owners closed. Session/transport/queue/routes/cookies/storage close with zero final work; no settlement wait; private HOME/TMP remain empty0700. Empty arrays were not used as owner-cleanup proof.
- Bounds held:8-GET maximum,250ms pacing,30s+5s,6MiB file/output,12MiB lane/64MiB preflight free; unchanged native DOM/style capacities, origin/public-address/TLS/privacy/identity protections and no page scripts. No credentials/providers, SafeJS, TTY/device, OS-font query, alternate browser/client, capacity raise, production/shared-document edit or commit.
- **43/43 named evidence checks passed; flow success remains false.** All previous reports/lanes/locks and audited source/compiled pins remain unchanged. Exact failure classification and limitations are in `OUTCOME-ANALYSIS.json`, with original stdout/progress, `OBSERVATIONS.json` and `VERIFICATION.json` in `node_modules/.cache/native-validation/native-curl-documentation-flow-september11`.
- Seal: lane-relative `RECEIPTS.sha256` and repository-relative `FINAL-RECEIPTS.sha256` cover the new evidence/report. Unresolved: selected-link actionability, recorded native image policy denial, and the independent CSS/formatting limitations; no successful FAQ navigation is claimed.
