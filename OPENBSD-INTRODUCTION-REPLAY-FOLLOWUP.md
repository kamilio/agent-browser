# OpenBSD introduction: corrected offline replay

- Outcome: **NATIVE FORMATTING GUARD FAILURE** on the one genuine `BrowserSession.click` invocation; zero wire, no further retry. This is captured-document replay, not a fresh website visit.
- Sole follow-up child: 2026-09-11T23:19:06.221Z–2026-09-11T23:19:06.415Z, exit 1, process group 3358872 absent, output53083 bytes, stderr0; no timeout, signal, overflow or stream error.
- Nine syntax checks and exact audited preflight passed before GO. Runtime commit `8936f29698f8f97d7ec4ac309d7459c6c2764539`; historical **10715 passed / 0 failed / 2 excluded**, not rerun; 182 selected, 181 strict, 583 manifest entries, 12 owned + manifest, actual 1071 source / 1912 compiled files.
- Only probe change: canonical sorted entries over all own header keys, preserving exact value arrays and array order. Preflight and seal verify this exact diff. Initial null-prototype assertion failure, report, stdout, locks and both ledgers remain sealed unchanged in `OPENBSD-INTRODUCTION-REPLAY.md` and its original lane.
- Inputs: original response3 HTML17530 SHA256 `35dc4df9a0665128437093852c42fa149ef66be9f8150ff6dae65b3b29da5f43`; original response4 CSS5325 SHA256 `25cd5cb07473f5b1be143e611df5332c0532c9cd4db0987d76896da0426ae80b`. Both replayed once with exact body parity and original captured headers/status/URL/redirect metadata; historical receive times/bytes remain labeled historical.
- Accounting: 2 adapter entries/admissions/bodyless native GETs/transport requests/mocks, 0 rejections/redirects/wire events, 22855 decoded bytes entirely mocked, 0 encoded bytes. One document committed; original CSS loaded as 1 external sheet, 83 rules / 135 declarations, with unchanged native budgets.
- Native discovery: 96 actual `body a[href]` results, exactly one valid Hardware Support anchor `e43`, href `#Platforms`, resolving to `h2#Platforms` reference `e187`; no forced historical reference. Page title `OpenBSD FAQ: Introduction to OpenBSD`, root `e1`, 574 nodes/revision574.
- Exact click failure: stage `native-hardware-support-fragment-click`, `AgentBrowserError`, code `unsupported`, “Document width resolution requires an issue-free supported formatting profile”. No click result or fragment navigation; native mouse actions0. URL, document/root, revision, history(index0,length1), scroll and request count remain unchanged.
- CSS diagnostics remain separate: raw `styles.metrics().issues` = `unimplemented-css-property:8`, `unimplemented-or-invalid-css-value:1`; applicable = `unimplemented-css-property:1`; filtered formatting CSS = `css:unimplemented-css-property:1` (non-advisory).
- Independent non-CSS formatting issues: `float-layout-not-supported:1`, `overflow-layout-not-supported:3`, all non-advisory; no table issue reported. Census: 562 visited DOM nodes, 668 boxes, 592 formatting nodes, 76 outside markers, 12251 text units, 15567 work, 0 deferred subtrees; revision574 unchanged. This does not establish font-weight as the sole blocker.
- Three actual read-only preference samples: `preference:null`, `effective:"light"`, `profile:"native-ua-color-preference"`, `systemIntegration:false`, `siteOverrides:false`; session preference also null. No preference setter/override.
- Cleanup: one actual document, event owner, image owner and file-control/mouse owner observed; all close with zero retained resources/work. Document nodes/text/inline declarations/collectors0; event listeners/dispatches0; image resources/active/queued/waiters0; controls/files/bytes/buttons/pressed0. Session, transport, queue, routes, cookies and storage close with zero work; no settlement wait required; HOME/TMP empty0700.
- Bounds preserved: OS socket-denying seccomp plus native/JS guards, guard attempts0; 30s+5s, 6MiB file/output, 12MiB lane, ≥64MiB preflight free, no scripts/credentials/device/TTY/alternate client/fallback/CSS pruning/cap change/production edit/build/commit. All prior evidence and released source/compiled inputs remain unchanged.
- Seal: **43/43 named evidence checks passed; replay interaction remains failed**. Private lane `node_modules/.cache/native-validation/native-openbsd-introduction-replay-followup-september11` contains exact stdout, census/observations, correction proof, verification and lane-relative `RECEIPTS.sha256` plus repository-relative `FINAL-RECEIPTS.sha256`.
