# Website inventory: September 14 twenty-sixth update

This append-only update adds **one captured attempt on one previously tested
host, zero new hosts and zero live requests**. It supplements the twenty-fifth
update and earlier inventories; it does not replace their measurements.

| Host | September 14 native UTC | Runtime | Actual outcome |
| --- | --- | --- | --- |
| `man7.org` | 15:24:52.577–15:24:53.765 | `71d0c8b59f7e92532eb956a972c3cb118789e9b7` | ls(1) commits; date(1) is rediscovered and activated; native destination request stops at a missing archive response before transport. |

The same workload previously failed first at percentage table width, then at
percentage cell-descendant height. With both focused fixes adopted, this check
gets past those guards and reaches the **actual destination-capture boundary**.
It is not a successful destination load, a live availability check or whole-site
acceptance. The missing response is not a new demonstrated browser regression.

Four unchanged original responses replay once each, totaling **39,562 decoded
bytes**. One initial navigate and one genuine discovered click produce two
session navigation attempts and one commit. Six adapter attempts consist of four
accepted mocks, the unchanged optional tracker denial and the uncaptured date(1)
request. The latter two do not enter transport. No wire, scripts, credentials,
providers/passkeys, devices, SafeJS, real TTY/PTY, forced click or fallback occurs.

Initial URL, document identity and history remain after the rejected navigation;
revision advances 727→732. Child/supervisor exit 1, while all **18 evidence and
cleanup checks pass**. The process group and private directories are cleaned up.
Verifier success is not site success, and timings are not performance benchmarks.

Report: `MAN7-FIELDSET-RECHECK-SEPTEMBER-14.md`.
Evidence: `/dev/shm/agent-browser-man7-fieldset-recheck-september14/`, with a
sealed byte-verified durable copy under
`node_modules/.cache/native-validation/man7-fieldset-recheck-september14/`.

The next useful action is a separately scoped fresh native live flow, not another
identical incomplete replay or fabricated response. Preparation is distinct from
launch permission and actual execution. Further diverse-site coverage, HN
word-breaking/layered-background support, original research and security/device
acceptance gates remain outstanding. No new host is counted for this repeat.
