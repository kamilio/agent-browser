# MDN positioning replay — September 11, 2026
- **First actual click failure:** `AgentBrowserError` / `unsupported`: **Outside list-item markers with block content are not implemented**. The original click error and stack remain primary in `RESULT.json`.
- One native `main a[href]` query observed 49 links and two destination matches; the first was `e1673`, `querySelectorAll()`, empty target. One unchanged `BrowserSession.click` ran before any optional diagnostic; no retry or direct destination navigation occurred.
- The old `e74` positioned-Grid error was **not emitted**; formatting threw before positioning dispatch. **This earlier list-formatting failure masks validation of the old `e74` guard/containing-block fix on MDN: no claim MDN passed that guard or measured a containing block.** See frozen `node_modules/.cache/native-validation/native-grid-positioning-september11-round01/snapshot01/src/formatting-tree.ts:748` and `node_modules/.cache/native-validation/native-grid-positioning-september11-round01/snapshot01/src/flex-document.ts:38`.
- **Real geometry was not reached:** completed geometry builds 0, rectangles 0, used styles 0, geometry work 0, cache revision -1 before/after. These counters do not deny attempted computation. Actual containing block and coordinates were not measured.
- The single optional post-click formatting attempt threw the same marker error; its separate `diagnosticError` did not replace the first failure. No completed formatting tree, current box/deferred counts, failing-node identity, or re-observed `e74` metadata was available. Earlier 1,753-box/54-node measurements are not reused as current results.
- CSS load succeeded: 18 sheets, 591 rules, 1,446 declarations, 84,329 code units, one cascade build and **2,894,159/5,000,000 work**. Property/at-rule/value/selector/media issue counts are 261/14/25/29/24. Revision remained 2750; no stylesheet/image admission limit was reported.
- Scope: only frozen `native-grid-positioning-september11-round01/snapshot01/dist`; actual gate independently verified **8,321 passed / 0 failed / 1 existing exclusion**, 137 selected / 136 strict, 554 manifest entries, 1,017 source / 1,812 compiled files. Gate finished 2026-09-11T14:58:45.792Z; no dirty worktree or later build was used.
- All 19 original responses retain ordered URLs/statuses/headers/body bytes (270,288 decoded bytes), original 1,280×720 viewport and original limits. Native transport correctly records **19 requests / 19 mocked requests**, zero encoded bytes, redirects and wire traffic; no uncaptured destination request was reached.
- Seccomp/JavaScript guards recorded zero attempts; no credentials, SafeJS, TTY, sockets, alternate browser, CSS removal, fabricated boxes, fallback submission, raw HTML search, capture mutation or source-snapshot duplication. The 30+5-second / 6 MiB policy held; child ran 15:05:47.151–15:05:47.546 UTC (395 ms), exit zero with empty stderr. Exit zero means procedure/cleanup completion, not click success.
- Session/transport/document/geometry closed; zero pending loads/storage events, active transport work, retained document nodes or cleanup errors. Native descriptors were unchanged with no overrides; private home/tmp were empty and removed, process group absent. Settlement was 0.5779 ms within 100 ms.
- Source inventory SHA-256 `6a55e6b64846fe4fe3fffe9d3da5b4447dfd9e5ade1006a23d4f39ee31b9745d`; compiled inventory SHA-256 `c04c414672491529afbec564f5e008423b955221018f8f87064c912c09c90fc1`. Complete pre/post/final file-only checks preserve these pins, 184 historical receipt entries and all nine preparation entries.
- Evidence: `node_modules/.cache/native-validation/native-mdn-positioning-replay-september11/RESULT.json`, `PREFLIGHT.json`, `RELEASE.json`, `INTEGRITY.json`, `VERIFICATION.json`, `RECEIPTS.sha256`; `formatting-diagnostic.json` is intentionally absent because no formatting result returned.
- The strict omission remains `src/snapshot.test.ts`; the one existing skipped test remains “exposes the separate total host-object ceiling without claiming full-pool runtime capacity.” Focused-test success and full-gate success are not website-rendering acceptance.
- Only this new report and private lane changed. Previous reports/evidence, production source, tests, manifests, `TASKS.md` and commits were untouched.
