# HTTPBin rendering prerequisites and remaining legends

September 13, 2026. Native groove borders and time-value display are implemented
and verified. The unchanged captured-form probe now traverses the two fieldsets
and time control, exposing two visible legends as the remaining observed layout
deferrals. **The genuine pointer submit still fails; no POST or echo occurs.**

This checkpoint adds no live request and no newly tested website. It reuses the
public HTTPBin form captured by the previous checkpoint, with its original body
and retained allowlisted headers. Full original-header parity is not established.
Historical loading and initial harness failure remain in
`HTTPBIN-NATIVE-FORM-SEPTEMBER-13.md`; they are not rewritten as new successful runs.

## Measured prerequisite attribution

A separate native captured-body inspection ran at 19:04:22.448–19:04:22.609 UTC on
the previous audited runtime: one load, three queries, one formatting inspection,
zero HTTP/actions and unchanged document revision 96. The formatting-call count
is established by the pinned probe and retained result, not a standalone counter.

- Fieldsets `e28` and `e52` have four 2px groove borders, color RGBA240/240/240/255.
- Their visible direct legends are `e30` (Pizza Size) and `e54` (Pizza Toppings).
- Time input `e85` has min11:00, max21:00 and step900. Its initial value is empty,
  and the old runtime has no software-control rendering descriptor for it.
- The old formatting tree defers those two fieldsets and the time control; it
  does not yet traverse their descendants to expose the legend deferrals.

Native retained-source extraction at 19:09:19.831–19:09:20.113 UTC reads the CSS
Backgrounds and Borders groove definition: one parse, four queries, 25 selected
blocks and 3,583 excerpt text units from a previously captured 544,031-byte body.
It makes zero HTTP requests and performs no layout, raster, SafeJS or image fetch.
The excerpts support a carved-in lighter/darker relief; the exact RGB formula,
half-width division, alpha treatment and pixel ownership are explicit native
appearance choices, not a specification-mandated pixel oracle. The extraction
is a bounded selection, not exhaustive section coverage or fresh web evidence.

Implementation, pixel/geometry coverage and limits:
`PUBLIC-FORM-RENDERING-PREREQUISITES.md`. Feature commit: `7e2734d`.

## Unchanged-harness captured-form replay

The new-runtime replay ran **19:40:06.979–19:40:07.155 UTC**. The original 1,397-byte
body produces the same 96-node document and form `e11`: POST action
`https://httpbin.org/post`, 15 associated controls and submitter `e93`.

All eight synthetic preparations still pass: name, telephone, email, time12:30,
comments, medium pizza size and bacon/cheese toppings. Nineteen preparation events
occur. These are semantic native fill/check operations, not physical typing or
eight pointer actions. The genuine `session.click` is attempted exactly once.

| Formatting after preparation | Previous runtime | Groove/time runtime |
| --- | ---: | ---: |
| Visited DOM nodes | 43 | 91 |
| Formatting boxes | 53 | 112 |
| Text code units | 248 | 373 |
| Work units | 555 | 1,032 |
| Deferred subtrees | 3 | 2 |
| Deferred references | fieldset e28, fieldset e52, time e85 | legend e30, legend e54 |

The increased traversal reflects newly admitted real subtrees, not faster work or
diagnostic filtering. The two remaining legends still stop width resolution:
`Document width resolution requires an issue-free supported formatting profile:
element-layout-not-supported (2)`.

No pointer/submit event is dispatched, no outbound POST intent reaches the adapter,
and no echo-verification or outbound-denial branch is exercised. The generic
control-type helper reports `text` for legend nodes in diagnostics; this is not an
HTML input type or a claim that legends are text controls.

The fixture, probe, runner, transport guard, network guard and kernel-seal helper
are byte-identical to the corrected previous offline probe. Only runtime pins
and the new authorization record differ. The form, prepared values and event
records also match. No body/CSS rewrite, asset removal, fabricated geometry,
semantic-submit fallback or fake response is used.

## Validation and containment

The final isolated native gate records **20,952 passed / zero failed / two
unchanged skips**, with build, strict types, scoped formatting and source checks
passing. It runs 19:35:05.248–19:39:58.997 UTC, selecting409files/408strictroots from
763manifestentries, leaving354unselected. The final focused comparison is
old1070/99/1 versus candidate1169/0/1. Earlier failed preparations, focused
assertions, output-buffer overflow and stale fieldset tests are all preserved and
explained in the feature document; no failed lane is relabeled as passing.

Runtime: `native-public-form-rendering-september13-round02/snapshot01/dist` under
`node_modules/.cache/native-validation/`. Its 1,304 source/2,124 compiled files
are pinned; 1,289 unowned tracked inputs match parent
`2ed8556f76ee78eb36aed604530a889c522e0600`.

- Audit SHA256: `7308ce62c2732d6cd62808391f110c2ec12043eae8fa00f0c28454844abd0d5f`.
- Source inventory: `d25d1ad2874f89b3d86fd608dbe493dd50d137e112859a32f776671cbe1b576f`.
- Compiled inventory: `e1da26799810739a6d76f5787b1975e26bbaa6e958298e922997f4b75134bf45`.
- Twenty runtime receipts: `215aa98b779e7a4c161fe5bd897a42c803ae965321514e72ac6eda2eb6f9ec93`.
- Thirty attribution entries: `9aac105ba407656b79aa40867901f414b562cb9ded520d0ad8331cda951c1289`.
- Forty-six groove-source entries: `928c6dc07395aa719bd746d3055e824edbc6ba99e73349fc6a5732801aaa1c47`.
- Twenty-eight replay entries: `63375ffdae664d1a724778128d7c7f3d86ce6b7dafdae4e0382f1df232748834`.

Replay evidence is in `native-httpbin-form-rendering-prerequisites-september13/`;
attribution and source evidence are in `native-httpbin-form-style-september13/`
and `native-groove-source-september13/`, all beneath the same validation cache.
Before/after source, compiled, framework and fixture pins are verified. Session,
document, image, query, event, transport and queue ownership close cleanly; private
HOME/TMP directories are empty and removed. Recorded groups878455/881411/901184
are absent. Offline guards, 30s plus5s watchdog and10MiBoutput allowance remain.

The replay's single instrumented observation is0.17s and90,656KiB peakRSS
(88.53MiB). It is an incomplete flow, not a repeatable benchmark or speedup.
There is no credential/provider, passkey-device, SafeJS, realTTY, listening-socket,
alternate-browser, fingerprint-disguise or challenge-bypass probe here.

## Next acceptance work

Implement actual visible-legend selection/ownership, intrinsic sizing, placement
and interrupted fieldset-border paint, using the existing native fieldset source
rules. Then repeat the unchanged captured-form pointer path before considering a
fresh bounded live echo. Do not bypass the observed layout guard.

Broader website functionality, access-block handling, repeatable performance,
research and separately gated credential/device/runtime checks remain incomplete.
The overall browser goal stays **ACTIVE**; TASKS.md retains the outstanding gates.
