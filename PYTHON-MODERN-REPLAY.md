# Current-release Python captured-resource replay — September 12, 2026

## Result: blocked before formatting

**The current native loader requests an original CSS import missing from the authorized seven-response capture.** At `2026-09-12T18:55:10.875Z`, request four targeted `https://docs.python.org/3/_static/basic.css`. The exact-capture adapter rejected it, aborted navigation, and stopped the one permitted native attempt. No resource was fabricated, omitted from the original source, fetched live, or substituted; no native retry occurred.

- Confirmed cause: captured `classic.css?v=234b1a7c` contains the exact declaration `@import url("basic.css");` at line 5 of `node_modules/.cache/native-validation/native-python-docs-queue-flow-september11/response-4.body`. Its SHA-256 is `94a00e0d0665461f189271938d429f1d92f5d5e3d8d54293346280b8144fefb5`.
- The captured index links it with the exact element `<link rel="stylesheet" type="text/css" href="_static/classic.css?v=234b1a7c" />`. `BLOCKER.json` preserves both exact samples, their source offsets, hashes, and element attributes. The authorized original response metadata contains no `basic.css` URL.
- Error ownership: **harness exact-resource allowlist**, `AssertionError / ERR_ASSERTION`, stage `offline-native-navigation`, message `Request has no unchanged original response fixture`. This is a capture-completeness blocker, not a native used-layout exception or proof of a website defect.
- Exactly **1 navigation attempt, 0 document commits, 0 formatting builds, 0 coordinator attempts, 0 clicks**. Required formatting/coordinator work could not be reached without violating the explicit missing-resource stop condition.
- Current raw stylesheet issue counts, applicable diagnostics, and table/flex/grid decomposition markers are **unobserved, not zero**. No used-layout guard was invoked or thrown. The historical width rejection and September 11 issue counts are not copied as new findings. Only one root-cause candidate is established; further layout candidates are not inferred.

## Runtime and accounting

- In-place release: `node_modules/.cache/native-validation/native-empty-image-september12-round02/snapshot01/dist`, commit `5164b28a6480c2748a18534ac7799ad0e215bfd1`; pinned Node **22.22.0**. No current working-tree source or fresh build was used.
- Existing gate verified, **not rerun**: **15,421 passed / 0 failed / 2 documented exclusions**, 295 selected native files, 294 strict roots, 673 manifest entries, all four gate commands successful. Source inventory **1,181**, compiled inventory **1,992**, **13 committed snapshot inputs / 16 Git objects** verified before/after/current.
- Source inventory SHA-256: `0d032903dd0004267cd30134bb6045ebabecb4e345c1b9dd09daa2d2da1895c8`; compiled inventory SHA-256: `b45c9a79c82dd9603952c4de3838d8412f385c1a1673e8337c31a425146e25eb`. Original seven response bodies, release/gate receipts, helpers, and the three required historical reports remain byte-identical; 105 additional pinned files match before/after/current.
- Native supervisor UTC: **18:55:10.728–18:55:10.934**, exit **1**, no timeout, no spawn/output/budget failure, stderr empty. Combined child stdout/stderr **10,149 bytes**.
- **4 adapter request attempts / 3 native routed transport responses / 0 wire requests or bytes**. The index, `pygments.css?v=b86133f3`, and `classic.css?v=234b1a7c` replayed **29,572 decoded bytes**. The rejected fourth request never reached transport. All seven archived bodies were verified, but only three were served.
- Underlying/advertised concurrency remains **1**, same frozen limits object, native queue maximum pending **128**. Original transport caps remain timeout 15,000ms, response 2,000,000 bytes, request 1 byte, headers 16,384 bytes, redirects 5, requests 12, total bytes 8,000,000. Original session/navigation and resource/cascade limits were not raised.
- Boundaries: parent **45s**, native child **30s + 5s termination grace**, output/file **6MiB**, lane **16MiB**, minimum free **64MiB**, diagnostics **64 samples / 32KiB**. The readonly blocker review contains **2 samples**; it executes no browser/formatting/parser code. Verification observed more than **4.49GB free** and a lane under **0.2MiB** before final sealing.

## Isolation, lifecycle, and retained failures

- The native child used unchanged `sealed-exec.py` and JS network guards, verified seccomp mode 2 / no-new-privileges, file limit 6MiB, core limit zero, non-TTY descriptors, and denied native addons/SafeJS/process spawning. The supervisor used the historical JS-guarded parent / kernel-sealed child architecture. Private sanitized HOME/TMPDIR are under this lane in `/home`; both are empty after cleanup.
- No live request, socket self-probe, SafeJS execution, scripts, credentials/providers, alternative browser/dependency, source-heading extraction, CSS rewriting, source/test/manifest/TASKS/Git edit, or production repair occurred. Zero-wire accounting is established by exact native routes plus guards, not packet capture.
- Immediate cancellation retained **853 parsed nodes**, **1 pending load**, and **1 active native queue entry**. One existing bounded settlement sample waited **49.497ms** (requested 50ms). Final nodes, tabs, pending loads, active transport, active/pending queue, cookies, storage entries, pending storage events, and cleanup errors are **0**; session/transport/queue are closed. Process group **28815** is absent.
- No committed document revision or diagnostic revision was observed. Image-owner registration occurs only after successful loader completion; this aborted path retained **no image owners**, so independent zero-image-state measurements are **not claimed**.
- Setup evidence is preserved, not overwritten: initial shell heredoc failed on its default temporary filesystem before harness creation; later setup used private `/home` TMPDIR. One pre-freeze patch was rejected for a missing marker. The first frozen supervisor failed at **18:54:27.978Z** with `spawnSync /usr/bin/git EPERM` because sealing the parent also blocked required subprocess IPC; it launched **no native child**. `run-setup02.mjs` separately froze the historical child-only sealing pattern and performed the sole native attempt. Original frozen harness, setup lock, and failure receipt remain intact.
- Nine harness syntax checks passed across preparation and blocker verification. The original success-only `verify.mjs` remains frozen and was not run against this failed navigation. The separately frozen `verify-blocked.mjs` verifies the actual stop condition, capture/release identity, request counts, source samples, and settled cleanup without replaying anything.

## Evidence and next gate

Evidence lane: `node_modules/.cache/native-validation/native-python-modern-replay-september12`. Key receipts: `PREFLIGHT.json`, `POSTFLIGHT.json`, `INTEGRITY.json`, `INVOCATION.json`, `EXECUTION.json`, `stdout.jsonl`, `progress.jsonl`, `BLOCKER.json`, `VERIFICATION.json`, and `FINAL-RECEIPTS.sha256`.

**Readonly blocker diagnosis and evidence verification completed; native formatting diagnosis, website acceptance, geometry acceptance, and interaction acceptance did not.** Final readonly verification checks the frozen harness, retained evidence/report receipts, release inventories, Git objects, original captures, and process absence again.

Next focused gate belongs to Main: separately authorize and supply an immutable capture of `basic.css` and any required import dependencies, then authorize a new bounded replay against that explicitly expanded corpus. The resource set was not broadened here, and no production change follows from this missing-capture result alone. Changed paths are only this new report and the new owned lane; historical evidence and unrelated work are preserved.
