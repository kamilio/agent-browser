# Libjpeg captured replay after float-shell coordination

## Result

One bounded, zero-HTTP native replay of the three original captured Libjpeg
responses completed on committed **13501**. The genuine documentation click
now stops at the independent supported-profile width guard rather than the
former blanket float/container guard. **This is not successful documentation
navigation, full-page geometry validation, or fresh live website acceptance.**

- Native observation UTC: **2026-09-12T13:48:12.786Z–13:48:13.420Z**.
- Supervisor UTC: **13:48:12.578Z–13:48:13.435Z**, elapsed **0.855 seconds**.
- One homepage navigation, one committed document, one genuine current link
  click; no additional navigation, replay retry, fallback, or destination fetch.
- Three original decoded responses through explicit native mock routes:
  **1 HTML + 2 CSS, 30,178 decoded bytes**. Original encoded bodies total
  **10,690 bytes** and are retained only as historical provenance.
- **0 HTTP/wire requests, responses, encoded wire bytes, replay misses, network
  guard attempts, or new attempted hosts.** Native transport independently
  reports 3 requests, 3 mocks, 30,178 decoded bytes, and 0 encoded bytes.

Current native click result:

```text
AgentBrowserError [unsupported]
Document width resolution requires an issue-free supported formatting profile
```

The original13226 click instead stopped at:

```text
Float integration with flex, grid and table reflow is not coordinated
```

The guard-transition hypothesis is observed, not assumed. Diagnostic occurrence
counts do not decrease in this retained page, and resolving the former global
guard does not erase independent unsupported CSS/HTML profiles.

## Pinned inputs and original capture

Release commit: `025bae17d4aaecff5e914980b9b46f0edc2e309e`.
Runtime: immutable `native-float-shell-september12-round00/snapshot01/dist`.
Pinned executable: `/home/kjopek/.nvm/versions/node/v22.22.0/bin/node`.
No native tests, production implementation, or architecture review was repeated.

The harness verifies all **20 release receipts**, **1,155 source files**,
**1,968 compiled files**, and **13 actual committed snapshot inputs** before
and after native execution. The original gate records13501 passed,0 failed,
2 unchanged exclusions,259 selected suites,258 strict roots,653 manifest
entries, and1142 unchanged tracked inputs.

Sixteen actual local Git objects were captured outside the kernel-denied native
process: commit, root tree, src tree, and13 input blobs. The verifier checks
Git object hashes, tree links, audited ownership hashes, and byte identity with
the immutable snapshot. No Git command or subprocess runs inside the browser.

| Release artifact | SHA-256 |
| --- | --- |
| Source inventory | `a1a6b8c9f1bad92db4740f45d8fb7dadb2c91d1ee516c1f8775cdb26b886d296` |
| Compiled inventory | `245b8f4a4b78a1c75d34fb1a4a6fa708b2eefbdc1ed40b5036f205507af297fe` |
| Native gate stdout | `fcd0b07729a130294b2cb276b551d04ace082a7e48d37bc1520c062f3acc7fb5` |
| Gate summary | `e10da848cf843523745b03e5d860f13e3c7060e142b0f703bfa7399062eb586e` |
| Audit | `54730b07543af09c31a0880eb5d549f88e7a3b0662b7bd67e0e4608106b730c5` |
| Twenty-receipt ledger | `5fe05de3ebad29aa3862df8c5954add4dd3b58e20c964d3328dd79f0582ad043` |
| Commit verification | `7afc25e6622d523dfd1acf37349845b2396e8c5f046a41f87d6683fee44d077b` |

Historical input lane: `native-libjpeg-turbo-flow-september12`, original live
observation **2026-09-12T12:26:18.000Z–12:26:18.978Z**. Its unchanged136-entry
sealed ledger has SHA-256
`c99a77fecdfbab567e41cddefe0525541327b7415e87255e19c01e392d8b3f63`.
All ledger entries and the original report remain unmodified.

| Captured resource | Status | Decoded bytes |
| --- | --- | --- |
| `https://libjpeg-turbo.org/` | 200 | 14,637 |
| `https://libjpeg-turbo.org/pmwiki/pub/lib/pmwiki-core.css` | 200 | 10,554 |
| `https://libjpeg-turbo.org/pmwiki/pub/skins/vgl/pmwiki.css` | 200 | 4,987 |

Each decoded body is byte-identical to its original capture and reconstructed
from the retained encoded body. Original retained status, response headers,
redirect metadata, and receive-time provenance are checked. Ordered raw header
normalization is verified without opening a socket. Credential-header
omissions made by the original capture remain omissions; nothing is invented
or reintroduced. There are no image or documentation-destination captures.

## Current link and state

The original selection policy is byte-identical: native style/actionability
checks on at most96 body anchors before URL deduplication, then documentation,
docs/help/manual preference before guide/FAQ/about. This run inspects all
**49 anchors**, finds **16 eligible occurrences / 14 unique destinations**,
and selects the actual current **e124**, index11:

- Text: **Official Binaries: Supported Platforms and Other Notes**.
- URL: `https://libjpeg-turbo.org/Documentation/OfficialBinaries`.
- Native displayed/visible true, blocked null, ARIA-disabled false,
  complete10-level ancestry, target `_self`.

No historical reference or destination was forced. This is a discovered HTML
documentation candidate, not a binary download. The native click fails before
any destination request, so no uncaptured response or fallback is supplied.

Before and after the click, the same document remains at the homepage with
title **libjpeg-turbo | Main / libjpeg-turbo**, root **e1**, **475 nodes**,
revision **478**, and history **length1/index0**, key **h1-1**. These stable
fields match the original retained state. Current before/after native DOM
serialization is identical: **14,559 bytes**, SHA-256
`951efa6c8301423153bd1a320478cb404ffaafb7d4571b1f47b798ea822871c1`.
This is a current before/after serialization comparison, not a claim of a
previously captured native-DOM hash.

## Actual diagnostics and resource limits

| Observation | Original13226 | Current13501 |
| --- | ---: | ---: |
| Non-CSS formatting occurrences | 12 | 12 |
| Applicable CSS occurrences | 39 | 39 |
| Raw stylesheet occurrences | 77 | 77 |
| Deferred formatting nodes | 4 | 4 |

Current non-CSS counts: positioned coordination1, generic presentation hints5,
table presentation hint1, display-layout markers2, float-layout marker1,
element-layout guards2. These are occurrence counts, not twelve independently
proven hard blockers: coordination markers are distinct from missing support.
Applicable CSS is19 property +6 value +14 selector occurrences. Raw stylesheet
CSS is54 property +9 value +14 selector occurrences; raw and applicable sets
are not interchangeable.

The single bounded formatting census visits451 DOM nodes, reports468 boxes,
3554 text code units and7627 work, with4 deferred subtrees. These formatting
counts are **not used full-page rectangles or pixel measurements**. No
diagnostic stripping, partial-layout fallback, extra census, or sole-cause
attribution is used.

Original native document, stylesheet, image, and action loaders remain in use;
only transport resolution is the explicit captured-response mock route. The
captured CSP headers and current policy states remain intact. Four image
elements are observed broken with `policy-denied`, natural size0×0: e35, e330,
e346, and e443. No image fetch callback or image wire request occurs. External
image source strings do not count as contacted or newly attempted hosts.

Network limits remain32 requests,1 concurrent,2MiB per response,8MiB decoded
total,15-second request timeout. Session limits remain1 tab,2 navigation slots,
1 pending navigation,20-second navigation timeout. Document limits remain
50,000 nodes,depth256,2,000,000 text code units,1024 changes; formatting limits
and stylesheet limits exactly match the original recorded values. Scripts and
real SafeJS remain disabled; credentials are omitted and the cookie jar stays
empty.

## Containment, cleanup, and preserved preparation

The native process runs with kernel socket/socketpair/connect/send/receive
denial, NoNewPrivs, additional JavaScript network/process/addon denial, no
stdin or TTY, explicit environment, empty private HOME and workspace TMPDIR.
No socket self-probes occur. Caps remain45 seconds plus5-second termination
grace,6MiB combined output/per file,16MiB lane,64MiB minimum free space.
Actual native stdout is62,114 bytes; stderr is empty. No timeout, termination
signal, output truncation, or surviving process group is observed.

All six cleanup categories are proved from actual instrumented owners:
documents, images, events, controls, session, and transport. The document has
zero retained nodes/text, event listeners and dispatches are cleared, image
queues/resources/waiters are empty, control file/mouse owners are closed, and
session/request queues and transport are closed with no pending activity.

Every preparation failure remains disclosed in `SETUP-NOTES.md`. A read-only
schema inspection used nonexistent key `wireEvents` instead of `wire` and
failed without a browser. Initial kernel-denied preflight then stopped at an
incorrect generated `.sha259` ledger filename, UTC
**13:46:47.686Z–13:46:47.732Z**, exit1, before native module loading. Its
stdout/stderr/invocation/receipt and original helper files remain preserved.
Only the harness filenames and phase bookkeeping were corrected; separate
preflight01 passes. Actual Git capture and successful preparation were not
repeated. **Exactly one browser replay occurred**, after corrected preflight.

## Evidence handoff

Private lane:
`node_modules/.cache/native-validation/native-libjpeg-float-shell-replay-september12`.
The report and exact evidence are sealed by `ARTIFACTS.json`, `SEAL.json`, and
`FINAL-RECEIPTS.sha256`; `REPORT-CLAIMS.json` binds this report hash.
`CHECKS.json` records the release/capture and replay verification groups.
The separate read-only verifier rechecks the ledger, exact file set, report,
original capture, release snapshot, and retained execution evidence with
kernel networking denied and compiled-page-runtime imports prohibited:

```bash
lane="$PWD/node_modules/.cache/native-validation/native-libjpeg-float-shell-replay-september12"
env -i PATH=/usr/bin:/bin TMPDIR="$lane/tmp" /usr/bin/python3 -I -B "$lane/offline.py" verify
```

It performs no replay and leaves the lane unchanged. No historical report,
review, production source, shared task list, manifest, inventory, or Git commit
was changed. Parent independently verifies and handles integration.
