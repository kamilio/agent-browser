# W3C candidate reader replay — September 11, 2026

**The fixed reader completes the captured W3C native research workflow.** One
candidate navigation discovers 114 untruncated headings; two observed-heading
sections extract successfully. All three receipts retain the honest
`extracted-unverified` outcome and partial semantic-reader limitations. This is
offline acceptance, not a new live visit, rendered Grid support or completed
research.

## Scope and authorization

The user's existing approval covers this isolated, credential-free native check.
The earlier agent's statement that another authorization was required was
incorrect. `GRID-READER-OMITTED-LIST-REPLAY.md` and its failed run remain unchanged
as historical evidence. This distinct candidate-only lane corrects the harness's
accounting assumption: total requests include mocked requests. It does not rerun
the baseline, reset counters or reclassify that earlier failure as success.

## Result

- Child: September 11, 2026, 12:15:03.165–12:15:03.995 UTC; PID 2552409,
  exit zero, no timeout, signal, output-cap breach or stream error.
- Same 957,488-byte W3C capture, original URL/status/headers and unchanged
  `long-v1` admission bounds with `separate-omitted-raw-v1`.
- One native research navigation, one mocked response, total requests one,
  encoded bytes zero; no wire traffic or network/process-guard attempts.
- Outline: 114 entries, none truncated, 17,940 scanned nodes. Reader: 27,024
  tokens, zero tokenizer issues, 223,411 text and 400,572 output code units.
- Reader still omits styling, scripts and object fallbacks. It reports partial
  content and does not establish hidden-content or rendering semantics.
- Successful sections: observed heading 8.3, line-based placement (35,597-byte
  receipt), and observed heading 11.3, track-sizing algorithm overview
  (5,600-byte receipt). Each selects one heading section with zero network calls.
- Track-sizing-function and named-area topic patterns matched four and three
  headings respectively. Both were skipped as ambiguous, not guessed. Detailed
  sizing subsections and the remaining topics still need deliberate selection.

## Validation and preservation

The candidate's existing gate is independently reverified: 6,805 selected native
passes, zero failures, one existing exclusion; 112 selected files and 111 strict
roots. This follow-up does not claim a new full-suite run. Both immutable build
inventories, original capture/diagnosis and the previous failed replay ledger
remain stable. The parent also verifies the native receipts and observed-heading
provenance after the child terminates.

Network-denying seccomp and JS guards remain enabled; no SafeJS, sockets, real
TTY, credentials or other browsers are used. Session/transport close with zero
pending or active work, all observed documents close with zero remaining nodes,
hooks restore, the process group disappears, and empty private HOME/TMPDIR
directories are removed. The same 30-second timeout plus five-second grace and
6 MiB output/per-file bounds apply.

Artifacts: `node_modules/.cache/native-validation/native-grid-reader-candidate-september11/`.
`VERIFICATION.json` and `RECEIPTS.sha256` record the file-only verification.
No new normative conclusions, Grid implementation, MDN click success or broader
four-topic research completion are claimed.
