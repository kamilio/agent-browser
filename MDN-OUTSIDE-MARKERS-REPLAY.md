# MDN outside-markers replay — September 11, 2026
- **First actual click failure:** `AgentBrowserError` / `unsupported`: **Document width resolution requires an issue-free supported formatting profile**. The genuine observed `querySelectorAll()` link (`e1673`, first of two matches among 49 main links) was clicked exactly once; its first error/stack remains primary.
- **Formatting now completes.** The one post-failure diagnostic returned 1,930 formatting boxes, 23 outside markers, 1,897 visited DOM nodes, 14,541 text code units and 27,851 work. These are formatting structures, **not used geometry**; the earlier outside-marker exception was not emitted.
- **The positioned-layout coordinator was reached:** the click stack enters `layoutPositionedDocument` and its normal-flow call before failing in `resolveFormattingPageWidths`. Frozen `node_modules/.cache/native-validation/native-outside-markers-integration-september11-round02/snapshot01/src/out-of-flow-positioning.ts:147` checks positioned-Grid eligibility before the normal-flow call at line 191; this supports passage through that old eligibility barrier, not completed positioning.
- The diagnostic re-observed `e74`, `ul.a11y-menu`, no id, absolute/block, `contentMode: blocks`, not a Grid item, under Grid parent `e72`. The old positioned-menu error was not emitted. **Actual containing block, final placement and coordinates remain unmeasured.**
- Completed geometry builds **0**, rectangles **0**, used styles **0**, geometry work **0**, cache revision **-1** before/after. Layout was attempted and reached the coordinator; no completed page layout or click success is claimed.
- There are 68 deferred subtrees: 54 coordinated Grid/Flex shells and **14 non-coordinated nodes**, all retained within the 20-node diagnostic cap. Grid shells/items: 10/47; Flex shells/items: 44/65. Non-CSS issues: display 55, positioned coordination 4, position 4, element 13, overflow 18, presentation hint 1. The raw diagnostic retains them all; no harness filtering or marker substitution occurred.
- CSS remains partial: property/at-rule/value/selector/media counts **261/14/25/29/24**. At 1,280×720, 18 sheets yielded 591 rules, 1,446 declarations, 84,329 code units and **2,894,159/5,000,000 cascade work**, one build; revision stayed 2750. The unchanged width-profile guard still rejects unsupported issues; bypassing it is not rendering acceptance.
- Frozen scope only: `native-outside-markers-integration-september11-round02/snapshot01/dist`, independently verified **8,529 passed / 0 failed / 1 existing exclusion**, 141 selected / 140 strict, 554 manifest entries, **1,019 source / 1,820 compiled files**. All four gate commands exited zero with stable inputs, 15:48:47.429–15:50:38.274 UTC; no rebuild, source copy or later worktree build was used.
- One native navigation, one native click, one post-failure formatting diagnostic; all **19 original responses** preserve ordered URLs/statuses/headers/body bytes (270,288 decoded bytes). Transport: **19 requests / 19 mocked requests**, zero encoded bytes, redirects and wire traffic. No destination request or fallback occurred.
- Child ran **15:58:29.176–15:58:29.649 UTC (473 ms)**, exit zero with empty stderr; this means diagnostic/cleanup completion, not website success. Original viewport/admission/transport/cascade limits, 30+5 seconds, 6 MiB, seccomp/JS guards held; zero guard attempts, credentials, scripts/SafeJS, devices, sockets, TTY, alternate browser, CSS rewrite, capture mutation or retries.
- Session/transport/document/geometry closed; zero pending loads/storage events, active transport, retained document nodes or cleanup errors. Native descriptors were unchanged without overrides; private home/tmp were empty and removed, process group absent. Settlement took 2.3969 ms within 100 ms.
- Source inventory SHA-256 `d0df0057f00c6878873274b4ba1ec1fa37f03d1854f6f989eee066c30fce728a`; compiled inventory SHA-256 `66de418a9a8e638955b65d203041405f923e8957f0f9aba76fb01172983bc539`. Complete pre/post/final checks preserve these pins, **212 historical receipt entries** and eight preparation entries.
- Evidence: `node_modules/.cache/native-validation/native-mdn-outside-markers-replay-september11/RESULT.json`, `formatting-diagnostic.json`, `PREFLIGHT.json`, `RELEASE.json`, `INTEGRITY.json`, `VERIFICATION.json`, `RECEIPTS.sha256`. The existing strict omission is `src/snapshot.test.ts`; the single exclusion remains the total-host-object-ceiling case.
- Only this new report/private lane changes; production source, shared documentation, tests, manifests, `TASKS.md`, original captures, historical reports/failures and commits remain untouched.
