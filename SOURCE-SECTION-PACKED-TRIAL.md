# Native packed-attestation section trial

September8,2026. One fresh namespace-bound native operation extracts selected
section119 from the existing source10 receipt; a separate verifier admits the
new export. **No newGET occurs.** This is bounded lexical evidence from the
previously captured `https://www.w3.org/TR/webauthn-3/`, not a new freshness or
authenticity check, complete WebAuthn implementation, or device acceptance.

## Actual operation and artifact

Native operation **11:48:11.296Z–11:48:11.768Z**, reported471.848025ms, exits0
with stage complete/codecValidation passed. Source supervisor runs11:48:10.949702155Z–
11:48:12.071979801Z; recorder11:48:10.740Z–11:48:12.260Z. Parent namespace
net:[4026531840] differs from active net:[4026532857]. Captured namespace process
11:48:11.273Z–11:48:11.781Z completes0, no signal/truncation. All associated
native/helper/input/frozen/terminal statuses are0; captured streams are empty.

Target119 is level3, with startTag1071753..1071827 and endTag1071980..1071985.
Native bodyRange1071985..1085765 ends at next same-level heading122, TPM, whose
complete closing anchor is1085983..1085988. Deeper120/121 remain inside the one
selected section. There is no host range slicing, alternate target or extra export.

New `section.jsonl`: **15017bytes**,59blocks including two deeper headings,
7302retained UTF16 units, validatedThrough1085988, textTruncated:false. Identity:
`f427cce5de09b39bfaf8e912b247e658bc923e481f6696f7bb14c71b83ae0b2b`.
Reported counters:47007tokens,47020operations,3971072work units including71504
projection work,0issues,216yields, maximum tracked depth5 and122heading starts.
Raw-discard counters record12style elements/27041units in12steps plus one legacy raw call.
No selected-body omissions are reported; this is not complete DOM/visibility proof.

## Separate verification and input scope

Verifier report **11:56:23.826Z–11:56:24.061Z** admits integrity and section
availability; timeout-command/recorder/outer statuses are0, streams empty.
It checks18actual inputs plus1declared old receipt,1748compiled engine files and
61root-run files. It performs no native reexecution and reports oldReceiptBytesRead0.
The original native outcome remains extracted-unverified, partial:true and
contentSuccess:null; separate admission does not rewrite it or old readiness flags.

- SOURCE19 ledger, independently fixed before source:
  `b538fef6f1cd4549079953ff2b159a0d1c6f04449ba329ab102a904956fd42cd`.
- Actual RUN61 ledger:
  `3c87d7cdbefbe8e1239ac577f910da7198666a0070c08378a724f7c14d9757ab`.
- INTEGRITY5517bytes:
  `a8a45587d2017c891f1fffd6827253b3fec12e0d69d9c7dbdac8ac36a961fe2a`.
- Frozen engine/shared02 inventory1748:
  `9d23cc14500ec63d575f8c9a97fb3baab36f36aef3d13fb767c05a6cb4251bf7`.

Unlike the historical trial02 scope qualified in SOURCE-RECEIPT-HASH-SCOPE.md,
trial03's outer checker validates all raw ledger bytes/order before hashing only
18ordinary inputs. The old receipt row stays literal/declared-only in pre/post
checks. Its actual read/hash/native decode occurs only inside the approved source
namespace. Pinned code plus observed checks support that distinction, not a global
syscall trace or hostile same-user alias isolation. Old trial02 evidence is unchanged.

## Admitted packed evidence

Only after successful verification does the parent read the new section prose.
Its normalized syntax identifies packedStmtFormat within attStmt, with a
certificate-bearing alg/sig/x5c alternative and a self alg/sig alternative
(1072793..1073251). The certificate array places the attestation certificate first.
This resolves the earlier missing nested certificate-member placement, not an
alternative fourth top-level envelope field or a complete executable CDDL grammar.

Self signing uses the credential key and its algorithm (1075852..1076241).
The absent-x5c verification branch requires agreement with the credential-public-key
algorithm and actual signature verification over authenticatorData plus clientDataHash
(1078715..1079814). The staged none-attestation helper performs structural shaping,
not those cryptographic verification steps or certificate/trust-path validation.

The two deeper headings include certificate and enterprise requirements. The
enterprise serial-extension discussion describes device-specific material stable
through factory resets and prohibited in non-enterprise attestations. The public
certificate example is only encountered as opaque source prose, not decoded,
verified or used as a key. No real device or credential is examined.

These observations support the earlier create-section conditional self exception
and nested x5c interpretation. They do not remove the helper's provisional label:
generating/none sections remain unread, strict CTAP/canonical/key/size subsets and
error precedence are local choices, and broker integration is a separate contract
change. No universal AAGUID-zeroing or complete anonymity guarantee is invented.

## Controls, chronology and remaining gates

Independent review fixes the initially missing supervisor contract and NUL/order
ledger issues before execution. Separate syntax10/10, synthetic checker94/94 and
unchanged native23/23 controls pass, with failures/unrun counts0. Checker cases
exercise both conditional-preflight and finalization contexts, including18actual
hash calls and a declared-only receipt sentinel, not a real source launch.

Each gate has its own exact approval. Syntax, checker and verifier approve first
try; native23 and source each have one approval-service timeout then one identical
approved retry. No actual operation is retried or limit raised. Source69bootstrap,
native31recording and verifier27recording input sets retain pre/post equality;
all full1748engine inventories remain unchanged. No engine rebuild occurs.

Source155+10s, capture125+5s and original120s core bounds remain, effective native
timeout119860ms,4096stream/65536section/8192result caps. Parent source recording
uses170+5s and outer185+5s for recording overhead, not extra source/native time.
Verifier retains30+5s with outer60+5s overhead; its empty streams have no new quota.
Outer startup/final publication are outside inner timers; publication is exclusive,
not atomic/durable, and namespaces are not full filesystem/process/IPC isolation.

Evidence remains in `node_modules/.cache/native-validation/native-section-trial-03/`.
Post-source proposals/results stay in child directories so the RUN root set is
unchanged during verification and final child-only sealing. Independent evidence/
interpretation review finds only a corrected raw-discard wording issue. Parent
separately rehashes all18ordinary SOURCE,1748engine,61RUN and69/27recorder inputs,
without reading the declared receipt; review itself rehashes selected inputs.

Final readable-file inventory `source-validation-01/FINAL-SHA256SUMS` has1058entries:
`7d91a37ecb08d73603e0d13818d8b2b3d14a133ba347672dae87c5b2b5d3e177`.
It covers1061regular files except itself and two intentional mode0002924-byte
synthetic ledger fixtures. Their paths/modes/sizes are recorded, but their contents
are NOT hashed or claimed verified. Three owned fixture symlinks are recorded
separately without following them. A first inventory attempt hits expected EACCES
and produces no ledger; it is preserved as failed bookkeeping, not a source failure.
The second explicit-exclusion audit passes1058checks without changing protected
modes or rerunning any source/validator. Read-only retention is not OS immutability.

No unread source, device, vault, SafeJS, real consent, signature trust, full-suite
or previously denied gate is accepted by this trial.
