# Python image fallback predicates — September 11, 2026

One additional offline native navigation and formatting build uses the same seven
unchanged captured Python responses and immutable 8,339-pass build. It measures
fallback predicates omitted from `PYTHON-DOCS-FORMATTING.md`, without changing that
earlier report or claiming new website interaction/geometry acceptance.

- Native document mode is **no-quirks**. All three actual `img` elements have a
  **present, nonempty `alt="Python logo"`**, no width/height attributes, computed
  width/height `auto`, and computed inline display.
- `e283` and `e759` are displayed and still deferred by unsupported-element
  formatting. `e103` has native `displayed:false` and no formatting node; this
  supplies new evidence for its omission without rewriting the earlier uncertainty.
- All three remain **broken / unsupported / complete / undecoded**, sharing the
  original fetched SVG. Native natural width/height and decoded pixel bytes are
  zero. No decode or replacement-image operation is performed.
- These measured predicates match the proposed bounded final-broken, present-alt,
  no-quirks text fallback. That proposal is not an implemented or site-tested fix
  in this run; full state-model semantics and SVG decoding remain separate.
- Supervisor UTC **16:08:40.891–16:08:41.098**, exit zero; exactly seven native
  requests, seven mocked transport responses, **zero wire requests**. One native
  formatting build reproduces the same active issue counts and deferred nodes.
- **35/35 evidence checks pass**. Original source/build/capture pins and limits
  remain unchanged; session, transport, queue, image owners and document cleanup
  reach zero resources. Process group is absent and private home/tmp are empty.
- Existing socket-denying seccomp, JavaScript network/process guards, 30-second
  child timeout plus five-second grace and 6MiB output/file caps remain enforced.
  No page scripts, SafeJS, credentials, devices, raw-HTML search, CSS rewriting,
  width resolution, click or new live request occurs.

Evidence: `node_modules/.cache/native-validation/native-python-image-predicates-september11-round02/`.
An earlier preparation-only lane, `native-python-image-predicates-september11`,
retains its failed inherited-file-limit assertion. It launched no native probe;
round02 supplies the required hard/soft limits rather than weakening the check.
