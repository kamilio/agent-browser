# W3C table-document captured replay — September 11, 2026

- **Outcome: original page commits, but the genuine fragment-link click still fails.** This is one captured-content replay, not a fresh website visit or full-page rendering pass. No original resource is rewritten or omitted; no guard is bypassed.
- Runtime: immutable `native-table-document-integration-september11-round04/snapshot01/dist`, commit `2e88c1464b0975f1ad8a9e3e4eca122e648175b8`. Audited10123 native passes/0 failures/2 unchanged exclusions,170 selected/169 strict,574 clean manifest,1059 source/1900 compiled inputs; all25 candidate source/manifest blobs and20 gate receipts verify. No gate/build/audit script is rerun and no mutable working source is imported.
- Source inventory SHA-256 `eddbf8d3976c022f0286b48bd958dfceab99b8cb5df8810319ba9bc6f350049b`; compiled `ed56ad5206a7fa7eeac5fb8a4aa00c8f23a3e6421387507a3a50a5f3c1d467cf`; audit `0917ce1c345170f014885199f3abf475dbc3dba9f04285bcf5d635c2bc6fab22`. Actual in-gate audit source is pinned as `f877ff7fd87a2563a0bab0fdae2ae8ddc6ba6203484ab8ebd5ba2b1c4feaa611` before/after.
- UTC execution `2026-09-11T21:07:48.400Z`–`2026-09-11T21:07:48.634Z`; child exits1 for the recorded flow failure, with no timeout, signal, stream error or output overflow. Process group3223692 is absent. The parent closed a stalled preparation worker before any preflight/launch and completed the same single authorized harness; the delay was not a user-authorization barrier.
- Exactly3 native attempts/3 transport requests/3 original captured HTTP200 mocks,78310 decoded mock bytes,0 encoded new bytes and0 wire requests/responses/redirects. Initial document URL `https://www.w3.org/TR/CSS22/tables.html`, title `Tables`,2075 DOM nodes. Original HTML SHA-256 `a57bee3e57104001442a313f695fd2e0aff808d9fb56f6e5655a1a3256b9e05e` remains unchanged.
- The original `/TR/CSS22/style/default.css` digest is `dfc76974e7d607ee89a7feb6a6e443ee57b0ef380d73125ca1ba66f66795b870`; `/StyleSheets/TR/2016/W3C-WD.css` is `bbc3f6c965222686da742407dd46ed3182a03aadc99e958b100add5a87c3c8cd`. URLs, status, headers, MIME, CSP and all captured bytes are preserved. Original encoded sizes describe historical live traffic, not this replay.
- Native discovery inspects152 body anchors and finds52 matching same-document candidates; one resolution verifies actual link `e96`, text `17.5 Visual layout of table contents`, href `tables.html#table-layout`, and actual target `e855`. Exactly one real `BrowserSession.click` is attempted, with no direct navigation or synthetic-dispatch replacement.
- Exact first failure: stage `native-same-document-link-click`, `AgentBrowserError`, code `unsupported`, message `Document width resolution requires an issue-free supported formatting profile`. No native pointer events are emitted, fragment navigation does not commit, and URL/history/root remain unchanged at `h1-1`/index0/length1,`e1`,revision2078. Existing scroll metadata is unbuilt, not successful geometry or scrolling.
- The bounded post-failure formatting census now retains **5 coordinated table roots,40 rows and80 cells**. Roots preserve refs `e752`,`e1036`,`e1444`,`e1506`,`e1634`, each with automatic separate-border style,2px spacing,8 rows and16 cells. This is real structural progress over opaque deferred subtrees, not evidence that their final page geometry was accepted.
- Non-CSS active counts are still `display-layout-not-supported:5`, representing those coordinated shells. Retained raw diagnostics are `css:unimplemented-css-at-rule:1`, `css:unimplemented-css-property:8`, `css:css-import-not-loaded:1`, `css:unimplemented-or-invalid-css-value:1`. Static inspection of the released width guard shows these non-advisory CSS diagnostics still reject the profile even when shell counts match; the non-CSS census is not the complete guard decision. No CSS suppression or partial-layout fallback is attempted.
- Census metrics:2203 formatting nodes,2269 boxes including66 outside markers,2040 visited DOM nodes,51563 text units and74720 work; DOM revision remains2078. Cell geometry is deliberately not requested after the failed click:0 samples, not zero-sized cells or a geometry pass. Further table layout on this complete original page remains unvalidated.
- Nine original image states remain denied by the native blanket-CSP capability limitation, with no image request; the original CSS import remains unloaded. These are native limitations, not nine server denials or a Cloudflare challenge. No CAPTCHA, access-control or credential boundary is bypassed; no fresh server classification is inferred from mocks.
- Caps remain one child/native capacity1,3 captured requests/0 wire,30s+5s,6MiB output/file, existing response/aggregate/DOM/style/work bounds, socket-denying seccomp and network/process guards, private clean HOME/TMP,64MiB minimum free and12MiB lane cap. No alternate browser/client, page scripts/SafeJS, credentials or real device/TTY use occurs.
- **49/49 evidence checks pass, not the website flow.** Both source/compiled inventories and all pins remain unchanged; final documents, session tabs/pending loads, transport/queue work, event listeners/dispatches and image owners are zero/closed, HOME/TMP empty. Prior evidence is preserved. Final report and both receipt ledgers are sealed in `node_modules/.cache/native-validation/native-w3c-table-document-replay-september11/`; attempted-host inventory remains63.
