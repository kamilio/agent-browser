# Public native checkbox example — September 11, 2026
- Outcome: **native semantic control behavior passed; hit-tested pointer acceptance failed**. One fresh bounded flow only; neither HTTP 200 nor semantic `click` events establish a universal site/pointer pass.
- UTC supervisor start/end: `2026-09-11T16:15:29.454Z` / `2026-09-11T16:15:30.488Z`; native child: `2026-09-11T16:15:29.537Z` / `2026-09-11T16:15:30.478Z`; exit `1`, no timeout/retry.
- Pinned `node_modules/.cache/native-validation/native-outside-markers-integration-september11-round03/snapshot01/dist`: historical 8643 passes / zero failures / one exclusion, 144 selected / 143 strict, 1019 source / 1820 compiled files; four stable exit-0 commands at `2026-09-11T16:00:25.845Z`–`2026-09-11T16:02:32.744Z`; all 16 scoped inputs independently matched committed `f74b7a1713ea7a4bf594c8cb62a49607da579cab`.
- Source inventory SHA-256 `c2277eb6ce02ddc0c0b16f2e969ffee84f3789a2b18021e0aaa91301148b3be0`; compiled `e2a6e5cf90d2a4e209d11feef85edf83e755e71021b56dcd3e007243b98add8a`; before/after/current and gate hashes verified without rebuilding, copying, or importing dirty image-fallback code.
- Exact requested URLs, all HTTP 200: `https://the-internet.herokuapp.com/checkboxes`; `https://the-internet.herokuapp.com/css/app.css`; `https://the-internet.herokuapp.com/img/forkme_right_green_007200.png`; `https://the-internet.herokuapp.com/css/font-awesome.css`. Only the initial document and original native stylesheet/image requests were admitted; no follow-up navigation or cross-origin request occurred.
- Accounting: four native attempts / four native-transport requests / four HTTPS calls and replies, zero mocked requests or redirects; 391809 encoded and decoded bytes. Unchanged response bodies are 2008, 353394, 7660 and 28747 bytes, with individual SHA-256/status/header/resource-provenance receipts in `stdout.jsonl`; captures remain bounded and unmodified.
- Native page committed and challenge classification passed: 81 DOM nodes, one native image loaded without recorded error. Query `input[type="checkbox"]` observed enabled `e56` initially false and enabled `e60` initially true; the first existing checkbox `e56` was selected, without inspecting irrelevant input values.
- `native-semantic-set-inverse` succeeded at `2026-09-11T16:15:30.404Z`: `e56` false→true. `native-semantic-restore-initial` succeeded at `2026-09-11T16:15:30.440Z`: true→false. Each native `interactions.setChecked` emitted ordered `click`, `input`, `change` events with matching real checked state/target; all recorded `isTrusted=false`. These are semantic activation results, not hit-tested pointer acceptance.
- Exactly one genuine `session.click` began at `2026-09-11T16:15:30.440Z`. Exact first failure at `2026-09-11T16:15:30.476Z`: stage `genuine-native-pointer-click`, `AgentBrowserError`, code `unsupported`, message `Document width resolution requires an issue-free supported formatting profile`.
- Pointer-stage control-event log is empty; last observed `e56` state remains restored false. No interaction continued after failure, no direct DOM/action substitution, guessed destination, CSS removal, fabricated geometry, or click retry occurred.
- Capacity preserved: adapter forwards the identical `transport.limits` object; native Session queue uses unchanged `maxConcurrent=1`, with no harness queue. Original 12-request, 2 MB/response, 8 MB aggregate, 30 s + 5 s grace and 6 MiB file/output caps retained; no credentials/providers, pageJS/SafeJS, alternate browser/HTTP, TTY/devices, POSTs, uploads or auth/account actions.
- **34/34 offline evidence checks pass**. Final document nodes/tabs/pending loads/transport activity/native queue activity and pending entries/event listeners are zero; session/transport/queue/events closed, private home/tmp empty, process group absent. All four close callbacks appear in `progress.jsonl`; the fourth follows the final JSON snapshot, which records three callbacks.
- Only new private lane `node_modules/.cache/native-validation/native-internet-checkbox-flow-september11/` and `INTERNET-CHECKBOX-FLOW.md` changed; prior evidence/reports and source/shared docs/TASKS/manifests untouched, no commits. Exact changed files: `node_modules/.cache/native-validation/native-internet-checkbox-flow-september11/CHANGED-PATHS.txt`; sealed ledgers: that lane's `RECEIPTS.sha256` and `FINAL-RECEIPTS.sha256`.
