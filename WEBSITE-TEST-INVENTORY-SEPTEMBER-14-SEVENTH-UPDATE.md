# Website test inventory — September 14, seventh update

**Adds a bounded diagnosis of the same captured MDN page, not a new live site.**
The earlier link click still fails. This run performs no interaction, geometry,
width resolution or destination navigation and does not alter earlier evidence.

One navigation, one default formatting build and one native hint query reveal
13 raw issue codes/255 overlapping occurrences. Bounded sampling uses127 calls
and retains22 directly deferred generated flex-item records,3 generated vertical
alignment candidates and5 hint candidates. Zero ordinary-inline records do not
mean absence:530 ordinary-inline candidates remain unexamined. The generated
queue leaves36 candidates unexamined. No claim of exhaustive attribution is made.

All19 original resources/270,288 bytes are served once; zero wire requests or
denials occur. The runtime remains ownership23e988d. The newer text-transform
optimization is independently tested, not validated on MDN by this older run.

The original prepared verifier's node/box equality assertion fails and stays
preserved. Supplemental and parent verification confirm2,047 nodes plus7 outside
markers equal2,054 boxes, and verify the remaining bounded observations, all18
ledgers and cleanup. See `MDN-FORMATTING-DIAGNOSIS-SEPTEMBER-14.md` for the caveat,
complete raw issue map, candidate references and evidence.

The independent performance change in `TEXT-TRANSFORM-SCAN.md` eliminates an
unneeded native transform scan for empty contexts and passes22,101 selected
tests, with two unchanged exclusions. That gate is not website acceptance.

Next: implement generated flex-item behavior with native regression tests;
preserve empty pseudo boxes and capability guards. Then recheck the original
captured action under a distinct scope and broaden public form/live-site flows.
The missing MDN destination, research, credentials/devices, SafeJS, socket, TTY
and challenge gates remain open. Overall browser goal stays **ACTIVE**. No push.
